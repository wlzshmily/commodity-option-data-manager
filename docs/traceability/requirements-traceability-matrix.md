# Requirements Traceability Matrix

| Requirement | Implementation Area | Tests/Evidence |
| --- | --- | --- |
| Current commodity option slice | collection, repositories, SQLite | unit tests, live collection reports, perf tuning evidence |
| WebUI overview and T-Quote | webui read model/app | WebUI smoke tests, local smoke command |
| Local API integration | api app | API tests, local smoke command |
| Credential safety | settings, API responses, reports, service logs, TQSDK CLI env handling | unit/API tests, safe log-context review, transient-env TQSDK attempts |
| Resumable shards | collection_state, market_collector | unit tests, live evidence, process-level shard tuning sample |
| TQSDK live reference update model | chain_collector, continuous_collector | `wait_update` absolute deadline unit test, live subscription tuning evidence |
| Tuned full-market catch-up | collect_market_parallel, collection_plan right-bound shards | shard-boundary unit tests, parallel CLI smoke/evidence, final local run 864/864 batches succeeded |
| Realtime Quote/Kline subscriptions | quote_streamer, quote_stream CLI | quote-stream unit tests, live smoke/evidence |
| Realtime Quote/Kline operator controls | api app, webui app, service state/logs | `/api/quote-stream` API tests, WebUI settings controls, compileall |
| Option/future expiry metadata for API callers | instruments repository, quote_streamer TQSDK field backfill, webui read model, API app | `tests/test_webui_read_model.py`, `tests/test_api_app.py`, `tests/test_quote_streamer.py` |
| Local diagnostics/logging | service_state, api app, webui read model/app | service log repository smoke, compileall, API/WebUI tests once dependencies are available |
| WEBUI-001 selector/localization/expiry-days change | webui read model/app | `tests/test_webui_read_model.py`; `uv run pytest -q` passed 46 tests; `uv run python -m compileall -q src tests`; agentic-SDLC check |
| WEBUI-002 T型报价 performance and expiry fallback | webui read model/app | `tests/test_webui_read_model.py`; `uv run pytest -q` passed 47 tests; `uv run python -m compileall -q src tests`; agentic-SDLC check |
| WEBUI-003 realtime Quote status clarity | webui app, service state | WebUI overview payload smoke; `uv run pytest -q` passed 47 tests; `uv run python -m compileall -q src tests`; agentic-SDLC check |
| WEBUI-004 overview realtime progress and exchange tabs | webui read model/app | WebUI overview payload smoke; `uv run pytest -q` passed 47 tests; `uv run python -m compileall -q src tests`; agentic-SDLC check |
| WEBUI-005 quote-stream stop and progress semantics | api app, webui app/read model | quote-stream API control tests; `uv run pytest -q` passed 47 tests; `uv run python -m compileall -q src tests`; agentic-SDLC check |
| WEBUI-006 Quote plus Kline runtime subscription progress | quote_streamer, quote_stream CLI, api app, webui app | quote-stream/API progress and timing tests; `uv run pytest -q` passed 50 tests; `uv run python -m compileall -q src tests`; agentic-SDLC check |
| WEBUI-007 overview progress wording and code-driven realtime ETA | quote_streamer, api app, webui app | WSL `smoke-local-app`; `python -m pytest tests/test_api_app.py tests/test_quote_streamer.py -s`; compileall |
| WEBUI-008 near-expiry subscription priority and marker | `quote_streamer.py`, `cli/quote_stream.py`, `api/app.py`, `webui/app.py`, `tests/test_quote_streamer.py`, `tests/test_api_app.py` | WSL targeted quote/API tests passed 16 tests; WSL full suite passed 62 tests; live restart verified `--near-expiry-months 2`, near-month progress `6870/15218`, and overview marker |
| PERF-001 IV window comparison and K-line cache splice | `cli/compare_iv_windows.py`, `klines.py`, `chain_collector.py`, `quote_streamer.py`, `cli/quote_stream.py` | WSL chain/quote tests; live IV window sample under ignored `docs/qa/live-evidence/iv-window-comparison/`; live cache-splice smoke requested `data_length=3` and completed with 0 errors |
| METRICS-001 realtime nonblocking IV/Greeks refresh | `metrics_dirty_queue.py`, `cli/metrics_worker.py`, `quote_streamer.py`, `api/app.py`, `webui/app.py` | `tests/test_metrics_dirty_queue.py`, `tests/test_quote_streamer.py`, `tests/test_api_app.py`; WSL full suite 61 tests; live API start/stop smoke |
| METRICS-002 realtime Kline coverage restoration | `api/app.py`, `webui/app.py`, `tests/test_api_app.py` | WSL targeted API/quote-stream tests passed 15 tests; WSL full suite passed 61 tests; live 4-worker restart verified no `--no-klines`, `--kline-data-length 3`, `kline_total=27386`, and Kline subscription progress reached 544 |
| KLINE-DEDUP-001 Kline subscription de-duplication and IV local splice | `quote_streamer.py`, `chain_collector.py`, `cli/metrics_worker.py`, `api/app.py`, `webui/app.py`, `tests/test_quote_streamer.py`, `tests/test_chain_collector.py`, `tests/test_metrics_worker.py`, `tests/test_api_app.py` | WSL full suite passed 66 tests; live/local probes showed wide multi-symbol Kline batches can block, so `--kline-batch-size` remains experimental, defaults to stable single-symbol setup, and is operator-configurable from settings |
| WEBUI-009 realtime network health detection | `realtime_health.py`, `quote_streamer.py`, `api/app.py`, `webui/app.py`, `tests/test_realtime_health.py`, `tests/test_quote_streamer.py`, `tests/test_api_app.py` | WSL full suite passed 73 tests after correcting normal Friday-night handling |
| WEBUI-010 TQSDK reconnect notification observability | `quote_streamer.py`, `realtime_health.py`, `api/app.py`, `webui/app.py`, `tests/test_quote_streamer.py`, `tests/test_realtime_health.py`, `tests/test_api_app.py` | WSL targeted realtime health/API/quote-stream tests passed 27 tests; WSL full suite passed 75 tests |
| WEBUI-011 realtime empty-database bootstrap | `api/app.py`, `webui/app.py`, `tests/test_api_app.py` | WSL targeted API/quote-stream/health tests passed 28 tests; WSL full suite passed 76 tests |
| CM-001 dynamic contract manager realtime baseline | `api/app.py`, `cli/quote_stream.py`, `quote_streamer.py`, `webui/app.py`, `tests/test_api_app.py`, `tests/test_quote_streamer.py` | Startup discovery always runs; runtime periodic discovery/reconciliation; WSL targeted API/quote-stream/health tests passed 29 tests; WSL full suite passed 77 tests |
| OPS-001 repeatable Linux server deployment | `docs/operations/linux-server-deployment.md`, `docs/release/release-plan.md` | Documents GitHub SHA releases, shared runtime layout, systemd service, verification gates, initial runtime start, public test smoke, upgrade, and rollback |
| WEBUI-012 SQLite-lock tolerant realtime controls | `api/app.py`, `service_state.py`, `tests/test_api_app.py` | API regression tests simulate locked service-state writes for quote-stream status/stop; `uv run pytest -s -q` passed 79 tests; remote running-worker stop smoke returned 200 |
| WEBUI-013 realtime Quote batch-timeout fallback | `quote_streamer.py`, `tests/test_quote_streamer.py` | Regression test simulates batch Quote timeout and one single-symbol timeout; targeted quote-stream/API tests passed; remote hotpatch smoke reached Quote 20/20 |
| WEBUI-014 realtime contract month scope | `quote_streamer.py`, `cli/quote_stream.py`, `api/app.py`, `webui/app.py`, `tests/test_quote_streamer.py`, `tests/test_api_app.py` | Settings expose `quote_stream.contract_months` as 1/2/3/all, default 2; Quote/Kline selection and dynamic reconciliation filter to the chosen option expiry months; near-month marker/summary is shown only for all-contract subscriptions; WSL full suite passed 82 tests previously; targeted quote/API tests, compileall, and agentic-SDLC check passed after marker gating |
| WEBUI-015 subscribed/current-slice WebUI semantics | `webui/read_model.py`, `webui/app.py`, `tests/test_webui_read_model.py` | Overview underlyings filter to Quote rows received after the current realtime run started; rows sort by exchange/product/delivery month; T型报价 reports `未订阅` until the selected chain has fresh realtime Quote rows; WSL `uv run pytest -s -q` passed 90 tests; compileall, local smoke, and agentic-SDLC check passed |
| WEBUI-016 near-contract minimum remaining-day filter | `quote_streamer.py`, `cli/quote_stream.py`, `api/app.py`, `webui/app.py`, `tests/test_quote_streamer.py`, `tests/test_api_app.py` | `quote_stream.min_days_to_expiry` defaults to 1 and is applied before each product's nearest underlying contract-month selection; regression test covers `pt2606` at 0 days advancing the 2-contract scope to `pt2608` and `pt2610`; targeted tests passed 31 tests; full suite passed 91 tests; compileall, smoke, and agentic-SDLC check passed |
| OPS-002 local server lifecycle script | `scripts/odm-server.sh`, `docs/operations/wsl2-ubuntu.md`, `docs/operations/linux-server-deployment.md` | Script supports `start`, `stop`, `restart`, and `status`; restart was verified locally and `/api/health` plus `/api/settings` returned 200 afterward |
| WEBUI-017 subscription scope and overview alignment | `quote_streamer.py`, `webui/read_model.py`, `webui/app.py`, `api/app.py`, `tests/test_quote_streamer.py`, `tests/test_webui_read_model.py`, `tests/test_api_app.py` | Option expiry remaining days now exclude invalid near contracts even when the underlying future delivery is later; overview underlyings are filtered by the same per-product nearest valid contract scope used by realtime Quote/Kline subscriptions; targeted suite passed 44 tests; full suite passed 94 tests; compileall and agentic-SDLC check passed |
| WEBUI-018 unified realtime data outlet and subscription lifecycle | `quote_streamer.py`, `api/app.py`, `webui/read_model.py`, `webui/app.py`, `tests/test_api_app.py`, `tests/test_webui_read_model.py` | API/WebUI progress totals are prefilled from the same SQLite subscription scope used by workers, so totals do not wait for worker report files; overview rows use lifecycle statuses `待订阅`/`订阅中`/`已订阅`; targeted suite passed 44 tests; full suite passed 94 tests; compileall and agentic-SDLC check passed |
| WEBUI-019 responsive realtime start/status | `api/app.py`, `webui/app.py`, `quote_streamer.py`, `tests/test_api_app.py` | `/api/quote-stream/start` returns `starting` immediately instead of waiting for full status assembly; running status uses worker reports directly and avoids redundant SQLite scope recalculation; worker scope reads retry temporary SQLite locks; targeted suite passed 44 tests; full suite passed 94 tests; compileall and agentic-SDLC check passed |
| WEBUI-020 enum-driven realtime state machine | `api/app.py`, `webui/app.py`, `tests/test_api_app.py` | Quote-stream lifecycle uses persisted enum values instead of localized message text for `blocked`/`starting`/`running`/`stopped`; frontend buttons/status cards read the enum; targeted suite passed 45 tests; full suite passed 95 tests; compileall and agentic-SDLC check passed; live local start reached 4 running worker PIDs |
| WEBUI-021 realtime overview lifecycle consistency | `webui/app.py`, `webui/read_model.py`, `sqlite_runtime.py`, `tests/test_webui_app.py`, `tests/test_webui_read_model.py` | Overview status uses enum-backed subscription lifecycle (`待订阅`/`订阅中`/`已订阅`) rather than fresh Quote write coverage; scoped summary counts align to worker progress; WAL runtime config keeps overview readable while realtime workers write; targeted suite passed 47 tests and live local overview showed 128 `已订阅` rows |
| WEBUI-022 realtime backend freshness during Kline setup | `quote_streamer.py`, `quotes.py`, `instruments.py`, `realtime_health.py`, `tests/test_quote_streamer.py`, `tests/test_realtime_health.py`, `tests/test_api_app.py` | Quote subscriptions now write current snapshots in bulk and keep refreshing snapshots while Kline objects are still being established; backend Quote refresh does not depend on any WebUI/T型报价 request; startup snapshots do not flood metrics dirty queues; health uses the freshest of worker report and DB quote writes; targeted realtime tests include no-UI backend refresh coverage; live active night-session `SHFE.al2606` source time advanced through `23:24:21`/`23:25:38`, while closed-session contracts kept source time near `22:59:59` |
