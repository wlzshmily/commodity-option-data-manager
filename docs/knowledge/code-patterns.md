# Code Patterns

- Repositories own schema initialization through migrations.
- API responses must never include raw secrets.
- WebUI display filters do not affect collection universe.
- Tests should prefer in-memory SQLite or temporary SQLite files.

