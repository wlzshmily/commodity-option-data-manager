# Project Status

- Current phase: Implementation
- Current release: v0.1 local production release
- Coding allowed: yes, for the approved implementation plan in this thread
- Scope: DCE, CZCE, SHFE, INE, GFEX commodity options
- API auth decision: default localhost with optional API Key enforcement
- WebUI decision: preserve existing WebUI as the approved baseline
- Runtime environment decision: WSL2 Ubuntu is the primary local development and debugging environment.
- Python runtime decision: use Python 3.11 for WSL/cloud runtime; Python 3.14 caused TQSDK HTTPS failures.

## Current Blockers

- No release-blocking data collection or display issue remains after the local tuned full-market run.
- Cloud-only live TQSDK runs remain blocked by outbound proxy denial to `auth.shinnytech.com` (`Tunnel connection failed: 403 Forbidden`); release acceptance evidence therefore uses the local direct TQSDK runtime.

## Verification

- WebUI T型报价 selector switching fixed; overview exchange labels are localized to Chinese; remaining expiry days appear in overview and T型报价.
- `uv run pytest -q`: passed, 50 tests.
- `uv run python -m compileall -q src tests option_data_manager`: passed.
- `uv run python scripts/smoke-local-app.py --database data/tmp-smoke/script-smoke.sqlite3`: passed for API/WebUI factory endpoints.
- `odm-api` server smoke on `127.0.0.1:18770`: `/api/health`, `/api/status`, and `/docs` returned 200.
- `odm-webui` server smoke on `127.0.0.1:18765`: `/`, `/api/webui/overview`, `/api/webui/runs`, `/api/settings`, and `/assets/webui.js` returned 200.
- `TQSDK_ACCOUNT`/`TQSDK_PASSWORD` transient-env `uv run odm-test-tqsdk --database data/live-tqsdk-check.sqlite3 --attempts 2 --retry-delay 2`: failed before authentication could complete because the runtime proxy denied the HTTPS tunnel to `auth.shinnytech.com` with 403; no credential value was printed.
- `TQSDK_ACCOUNT`/`TQSDK_PASSWORD` transient-env `uv run odm-collect --database data/live-smoke.sqlite3 --report docs/qa/live-evidence/2026-05-09-live-smoke-report.md --max-underlyings 1 --max-batches 1 --option-batch-size 5 --wait-cycles 1`: failed for the same proxy tunnel denial; the ignored report confirmed credential values were not written.
- `odm-test-tqsdk` now correctly reads process environment credentials when no test env mapping is injected.
- `scripts/agentic-sdlc/check-agentic-sdlc.ps1 -Root .`: passed.
- WSL2 Ubuntu setup path documented in `docs/operations/wsl2-ubuntu.md`; Linux SDLC checker added at `scripts/agentic-sdlc/check-agentic-sdlc.sh`.
- WSL setup guard verified: setup refuses `/mnt/c` Windows-mounted paths and requires a WSL-native clone.
- WSL live TQSDK connection test passed under Python 3.11.
- Standalone `odm-test-tqsdk` command added for credential/network checks outside the WebUI.
- TQSDK startup now retries transient login/TLS failures and reports non-zero collection exits to the WebUI status.
- Quote collection now prefers TQSDK `get_quote_list` batch subscription, and interrupted `running` batches are reset to `pending` on the next plan materialization.
- Short-window collection now passes absolute `time.time() + 1` deadlines to `api.wait_update()`, matching TQSDK's subscription update contract.
- API factory smoke test: `/api/health`, `/api/status`, and `/api/settings` returned 200.
- WebUI factory smoke test: `/`, `/api/settings`, and `/api/webui/overview` returned 200.
- WebUI/API status now expose collection batch progress, including success, pending, failed, remaining, and recent failed batches.
- Local service logs now persist API/settings/security/background refresh events and are visible from WebUI diagnostics plus `/api/logs` without exposing TQSDK passwords or full API keys.
- WebUI overview now separates exchange market time from local collector update time and renders UTC timestamps in Asia/Hong_Kong.
- Metric-only Greeks/IV source gaps no longer block collection batch success when current Quote rows are written.
- Live bounded collection smoke: `uv run odm-collect --max-underlyings 1 --max-batches 1 --option-batch-size 5 --wait-cycles 1` completed and wrote a partial-failure report without secrets.
- Live full-market planning smoke: `uv run odm-collect --max-underlyings 1000000 --max-batches 3 --option-batch-size 20 --wait-cycles 1` materialized 380 underlyings, 27,386 options, and 1,544 active batches; the selected 3 batches completed successfully without secrets in the report.
- WSL WebUI background full-market refresh is running with 380 underlyings, 27,386 options, and 1,544 active batches discovered; progress has started without failed batches.
- Local real TQSDK login check passed on Windows with encrypted SQLite credentials: `uv run odm-test-tqsdk --database data\option-data-current.sqlite3 --attempts 2 --retry-delay 2`.
- Tuned local performance evidence captured under ignored `docs/qa/live-evidence/perf-tuning/` paths:
  - `option_batch_size=40`, `wait_cycles=1` was the best single-process bounded-window candidate in the small sample.
  - 2 independent TQSDK worker processes improved throughput to about 6.078 options/second; 4 workers improved to about 7.827 options/second with sublinear scaling.
  - Quote-only probes showed much higher subscription/write throughput than full Quote/K-line/metrics windows, supporting a split realtime Quote worker plus lower-frequency K-line/metrics refresh.
- Tuned process-level full-market catch-up command implemented: `odm-collect-parallel` uses disjoint underlying ranges, independent TQSDK API processes, `option_batch_size=40`, and `wait_cycles=1` defaults.
- Long-lived subscription command implemented: `odm-quote-stream` uses bounded Quote/Kline shards and keeps TQSDK references alive through `api.wait_update()`.
- Local command smoke passed without writing secrets:
  - `uv run odm-quote-stream --database data\option-data-current.sqlite3 --report docs\qa\live-evidence\2026-05-09-quote-stream-smoke.json --no-discover --max-symbols 10 --cycles 1 --quote-shard-size 10` wrote 10 Quote rows with status `success`.
  - `uv run odm-collect-parallel --database data\option-data-current.sqlite3 --report-dir docs\qa\live-evidence\parallel-smoke --summary docs\qa\live-evidence\parallel-smoke\summary.json --workers 2 --max-batches-per-worker 1 --option-batch-size 40 --wait-cycles 1 --no-discover` ran 2 worker processes, selected 2 batches, and wrote 66 Quote rows plus 1,320 K-line rows.
- WebUI read model now prefers the latest parallel collection progress for operator display while API background refresh can still monitor the routine collection scope.
- Stale interrupted acquisition runs older than 30 minutes are marked failed before collection, preventing false long-running diagnostics; current runtime cleanup marked 3 old interrupted runs failed.
- Real runtime API/WebUI smoke returned 200 for `/api/health`, `/api/status`, `/api/webui/overview`, `/api/webui/tquote`, `/api/webui/runs`, and `/api/settings`.
- Final real runtime API/WebUI smoke returned 200 for API `/api/health`, `/api/status`, `/api/settings`, `/api/logs`, `/api/quote-stream`, `/docs` and WebUI `/`, `/api/settings`, `/api/webui/overview`, `/api/webui/tquote`, `/api/webui/runs`, `/api/quote-stream`, `/assets/webui.js`, `/assets/webui.css`; the overview reported 864/864 successful batches and 0 failed.
- Final tuned full-market local acceptance run completed without writing secrets: `uv run odm-collect-parallel --database data\option-data-current.sqlite3 --report-dir docs\qa\live-evidence\final-parallel-catchup\workers --summary docs\qa\live-evidence\final-parallel-catchup\summary.json --workers 4 --max-batches-per-worker 60 --option-batch-size 40 --wait-cycles 1 --until-complete --max-waves 10`.
  - Result: 864/864 collection batches succeeded, 0 failed, 380 active underlyings, 27,386 active options, 565,000 K-line rows, 27,386 metrics rows, and 27,386 Quote rows present in `quote_current`.
  - Summary: 4 worker shards, 6 waves, 4,325.332 seconds, 6.332 options/second, 28,250 Quote writes, 27,386 metrics writes, 573 metric-source errors recorded as retryable `EmptyMetricsError` quality gaps.
  - WebUI/read-model acceptance view after completion: `completion_ratio=1.0`, `success_batches=864`, `failed_batches=0`, `stale_batches=0`, K-line coverage `1.0`, IV coverage `0.9013`, Greeks coverage `0.8959`, price-field quote coverage `0.8553`.
  - Data-quality interpretation: every active option has a Quote row, K-line row set, and metrics row; remaining IV/Greeks and price-field gaps are source availability/market-state gaps surfaced in diagnostics, not collection failures.
- WebUI/API quote stream controls are implemented: `/api/quote-stream` status, `/api/quote-stream/start`, `/api/quote-stream/stop`, settings-page worker count/shard-size/max-symbol controls, cooperative stop-file worker shutdown, and safe service-log events.
- Targeted verification for quote stream controls passed; final full suite now passes 41 tests and compileall passed.
- Live quote stream control smoke passed with local TQSDK credentials: API started 1 worker with 10 symbols, stopped it through `/api/quote-stream/stop`, left no running worker process, and wrote a secret-safe runtime report under ignored `docs\qa\live-evidence\quote-stream-runtime`.
- Local acceptance UI/API polish completed after browser review: settings and overview page spacing/status/table issues resolved; API Key rows support full fingerprint display, copy, and delete; overview product labels are consistently Chinese; collection progress hides empty message areas.
- TQSDK same-name expiry metadata fields are now supported in `instruments` and API payloads: `expire_datetime`, `last_exercise_datetime`, `delivery_year`, `delivery_month`, `exercise_year`, and `exercise_month`. Realtime workers backfill those fields from live TQSDK quote objects when available; `/api/options`, `/api/underlyings`, and WebUI tquote payloads expose original fields plus clearly-derived remaining-day fields.
- Targeted final verification for local acceptance polish passed: `uv run pytest tests/test_webui_read_model.py tests/test_api_app.py tests/test_quote_streamer.py -q` passed 17 tests; compileall passed; local 8766 WebUI/API smoke returned 200.
- WebUI T型报价 switching performance follow-up completed: quote-chain requests skip selector re-aggregation during contract switches, latest K-line lookups are scoped to the selected chain, and remaining expiry days fall back to TQSDK fields stored in `quote_current.raw_payload_json` when `instruments` expiry columns are still null. Verification passed with `uv run pytest -q` at 47 tests, compileall, and agentic-SDLC check.
- WebUI overview realtime-status follow-up completed: realtime Quote worker count/PIDs now render on the overview progress cards, latest data write time is separated from batch-plan update time, and the batch card is explicitly labeled as batch progress rather than realtime subscription progress. WebUI SQLite connections now use a busy timeout so startup can wait for realtime writer locks instead of failing immediately. Verification passed with `uv run pytest -q` at 47 tests, compileall, and agentic-SDLC check.
- WebUI overview navigation follow-up completed: the collection progress section now includes a realtime subscription coverage progress bar based on active quote symbols with Quote data, the underlying summary table can be filtered by exchange tabs, and restarted WebUI/API processes can detect persisted live quote-stream PIDs. Verification passed with `uv run pytest -q` at 47 tests, compileall, and agentic-SDLC check.
- Realtime Quote progress semantics and stop behavior clarified: the overview progress bar now tracks quote-row subscription write coverage, while market-field coverage is shown separately as a quality metric that can remain below 100% when the source returns empty price fields. Quote-stream stop now checks persisted worker PIDs by command line, uses a short graceful wait, and terminates the process tree to avoid stale PID false-running states. Verification passed with `uv run pytest -q` at 47 tests, compileall, and agentic-SDLC check.
- Realtime subscription progress now matches the operator definition: `(current subscribed Quote objects + Kline objects) / (required Quote objects + Kline objects)`. `odm-quote-stream` writes runtime progress while creating Quote/Kline subscriptions, WebUI/API aggregate worker reports, stale reports are cleared on fresh starts, and stopped status returns zero progress so restarts begin from zero. The overview progress card also shows current stage, separate Quote/Kline bars, total subscription elapsed time, average seconds per subscribed object, and estimated remaining time. Verification passed with `uv run pytest -q` at 50 tests, compileall, and agentic-SDLC check.
- Batch IV refresh now uses 3-day incremental K-line fetch plus local 20-day cache splice when both the option and underlying already have at least 20 cached daily K-line rows; cache-missing contracts still fetch 20 days. The spliced frame includes the TQSDK `duration` field required by `OPTION_IMPV`. WSL live cache-splice smoke on `CZCE.AP611C8400` observed `data_length=3`, completed with 0 errors, and wrote finite IV. Realtime subscription defaults remain at 20 daily bars. WSL full test suite passed with 57 tests; compileall, local WebUI/API smoke, and agentic-SDLC check passed.
- Realtime IV/Greeks refresh now uses a nonblocking dirty queue plus independent `odm-metrics-worker`: option Quote price changes enqueue that option; underlying future price changes enqueue the active chain with interval limiting; metrics refresh is throttled by configurable `metrics.min_interval_seconds`, default 30 seconds. Quote workers do not call `query_option_greeks` or `OPTION_IMPV` synchronously. Empty/failed source metric payloads do not overwrite prior finite metrics. WSL full test suite passed with 61 tests; live API start/stop smoke launched and stopped quote plus metrics workers.
- Realtime subscription startup again includes both Quote and Kline objects. The prior Quote-only `--no-klines` attempt was rejected because daily K-lines update during the trading session and the required Kline object count is unchanged whether each serial requests 3 or 20 bars. API-started `odm-quote-stream` workers now pass configurable `--kline-data-length` (default 3) while preserving full Kline object coverage; IV calculation still uses the 3-day fresh fetch plus local 20-day cache splice and falls back to 20 days when cache is incomplete.
- WSL verification for the Kline coverage correction passed: targeted API/quote-stream tests passed 15 tests, full suite passed 61 tests, compileall and agentic-SDLC check passed. Live 4-worker restart showed commands with `--kline-data-length 3` and no `--no-klines`; `/api/quote-stream` reported `kline_total=27386` and Kline subscribed progress advancing.
- Near-expiry subscription priority is implemented and enabled by default. Realtime workers sort Quote/Kline subscription objects by nearest expiry month, settings expose the priority toggle and near-month count (default 2), and the overview progress bar marks the first N near-expiry months with a red boundary line. WSL full suite passed 62 tests; live 4-worker restart reported near-month progress `6870/15218`.
- Realtime Kline subscriptions now use de-duplicated single-symbol objects instead of per-option `[option, underlying]` pairs. IV refresh fetches option Klines and one underlying Kline per chain batch, then builds the local two-symbol frame required by `OPTION_IMPV`; metrics workers also group dirty tasks by underlying before refresh. Targeted WSL tests passed: 13 quote/chain/metrics-worker tests and 17 API/WebUI tests.
- Kline setup performance note: removing duplicate underlying data does not by itself reduce realtime Kline setup time when `get_kline_serial` call latency dominates. `odm-quote-stream` exposes `--kline-batch-size` for controlled multi-symbol serial experiments, but defaults to stable single-symbol Kline setup because local probes showed wider batches can block for some symbol combinations. WSL full suite passed 66 tests.
- Kline batch-size setting note: `quote_stream.kline_batch_size` is now surfaced in settings and passed through API-started realtime workers. Default remains `1`; setting it to `2` is an operator-controlled live experiment and should be judged from runtime progress/ETA before wider rollout.
- Realtime health follow-up: quote-stream workers now keep writing runtime heartbeat fields after subscription setup, including `last_wait_update_at` and `last_quote_write_at`. API/WebUI expose an `开盘网络` health card that only evaluates during broad commodity trading sessions and only escalates after current-session market-time evidence exists, so holidays and post-close periods do not trigger network alarms.
- Realtime health night-session correction: normal Friday night is treated as a possible night session and Saturday early morning can belong to that Friday night session. The holiday suppression now looks for statutory holidays ahead, while normal weekends do not cancel Friday night. Targeted realtime health/API/quote-stream tests passed 25 tests.
- TQSDK reconnect observability follow-up: realtime quote-stream workers now attach `TqNotify`, record official connection notifications (`connected`, `reconnecting`, `disconnected`) in worker progress reports, aggregate those fields in `/api/quote-stream` and WebUI overview/settings status, and prefer fresh SDK disconnect/reconnect notifications in the trading-session health result without triggering full resubscription. Verification passed: targeted realtime health/API/quote-stream tests passed 27 tests, compileall passed, and WSL full suite passed 75 tests.
- Realtime startup bootstrap correction: `/api/quote-stream/start` now checks for active option contracts before spawning realtime workers. On an empty/new database, the API performs one TQSDK contract discovery pass in the parent process, persists the active universe, and then starts Quote/Kline workers with `--no-discover` so discovery is not repeated per worker. If discovery fails or returns no active options, the API returns a blocked status with an explicit message instead of letting workers exit obscurely. Verification passed: targeted API/quote-stream/health tests passed 28 tests, compileall passed, and WSL full suite passed 76 tests.
- Dynamic contract manager baseline is implemented for realtime long-running operation. `/api/quote-stream/start` now always runs a parent-process contract discovery pass before worker startup, not only for empty databases. `odm-quote-stream` then keeps a runtime contract-management loop: worker 0 periodically refreshes the persisted TQSDK contract universe, and every worker periodically reselects its own active Quote/Kline shard and reconciles in-memory references incrementally by keeping unchanged refs, removing inactive symbols, and subscribing newly active symbols. Verification passed: targeted API/quote-stream/health tests passed 29 tests, compileall passed, and WSL full suite passed 77 tests.
- Linux server deployment documentation added at `docs/operations/linux-server-deployment.md`, covering GitHub SHA releases, shared runtime state, systemd, credentials, initial collection/realtime start, public test smoke, upgrade, and rollback.
- Realtime controls SQLite-lock hardening completed: `/api/quote-stream` status and stop no longer return 500 when best-effort service-state writes encounter a temporary SQLite busy/locked condition. Local full suite passed 79 tests; compileall and agentic-SDLC check passed; remote public test server start/stop smoke returned 200 and left no quote/metrics worker process running.
- Realtime Quote batch-timeout hardening completed after the public test server worker exited on a TQSDK `TqTimeoutError` while subscribing a GFEX Quote batch. Quote workers now fall back from batch `get_quote_list` to single-symbol `get_quote` subscriptions and skip only symbols that still fail, instead of terminating the entire worker. The public server was hotpatched, restarted, and smoke-tested with 20 symbols; local quote-stream/API targeted tests and compileall passed.
- Realtime subscription pressure reduction completed: settings now expose `quote_stream.contract_months` as a dropdown with `1/2/3/全部`, defaulting to `2`; near-expiry priority remains enabled by default, and Quote/Kline selection plus runtime reconciliation only subscribe the configured option expiry-month scope unless `全部` is selected. The near-month progress marker/summary is hidden for scoped subscriptions and shown only for `全部`. Verification passed in WSL: `uv run pytest -s -q` passed 82 tests previously; targeted quote/API tests and compileall passed after marker gating; agentic-SDLC check passed.
- Realtime overview visibility follow-up completed: the realtime subscription progress card now shows the effective contract-month scope, and shows `正在刷新合约列表` while startup contract discovery is in progress. Targeted WSL API/quote-stream/WebUI read-model tests and compileall passed.
- WSL runtime credential-read hardening completed: if an old runtime SQLite credential secret cannot be decrypted by the current WSL user/key, settings and quote-stream startup now return a blocked/readable status instead of 500; the operator can re-save the password in settings. Local WebUI smoke on `127.0.0.1:8765` returned 200 for `/`, `/api/settings`, and `/api/webui/overview`.
- Realtime start UX hardening completed: `/api/quote-stream/start` now returns immediately after recording `实时订阅启动前正在刷新合约列表`, and performs TQSDK contract discovery plus worker launch in a background thread. Settings buttons stay in a startup state while discovery is running, so the page no longer waits on a long TQSDK startup HTTP request. Targeted WSL API/quote-stream/WebUI tests and compileall passed.
- Realtime month-scope restart refinement completed: changing `quote_stream.contract_months` now reuses the existing active contract universe when available and only recomputes the concrete subscription list from local SQLite. Full TQSDK contract discovery is reserved for an empty active universe or an explicit discovery request, reducing startup pressure after routine 1/2/3/all month-scope changes.
- WebUI large-local-DB responsiveness hardening completed: current-slice tables now have read-model indexes for latest timestamp lookups, and WebUI overview/T-quote/runs endpoints use request-scoped SQLite read connections for file-backed databases instead of sharing the API control connection. This prevents a slow overview query on the WSL `/mnt/c` database from blocking settings, health, or realtime start controls.
- WebUI settings-page startup hardening completed: the root HTML no longer precomputes a 500-row overview before rendering. Hash routes such as `/#settings` now load the settings shell immediately and only fetch the heavy overview when the operator opens the overview page; overview polling is guarded against overlapping requests and reduced to a 30-second cadence.
- Runtime settings write isolation completed: `/api/settings/{key}` and `/api/settings/tqsdk-credentials` now write through a request-local SQLite connection under a write lock for file-backed databases, preventing concurrent overview/status reads from corrupting or interrupting settings commits during realtime startup.
- Realtime contract-month scope semantics corrected: the `1/2/3/all` setting now limits subscriptions by each product's nearest underlying contract months, matching what operators see in the overview's underlying table, instead of limiting by option exercise/expiry month. The overview renders an empty collection/subscription skeleton immediately, then lazily fills data, so the progress area no longer appears blank while the heavy overview query is still loading.
- Realtime first-run contract discovery separated from subscription start: settings now has a dedicated `拉取合约列表` action backed by `/api/quote-stream/discover-contracts`. `/api/quote-stream/start` no longer initializes an empty contract universe implicitly; if no active options exist, it returns a blocked status telling the operator that the contract list has not been pulled and行情订阅 cannot start yet.
- Runtime best-effort persistence hardening completed for the first-run workflow: service-state, service-log, and request-metric writes now ignore retryable SQLite busy/commit anomalies instead of turning settings or quote-stream actions into HTTP 500 responses. Verification passed with targeted API/quote-stream/WebUI/realtime-health tests at 44 tests, compileall, and agentic-SDLC check.
- Realtime first-run UX tightened after browser review: the settings action buttons for `拉取合约列表 / 启动实时订阅 / 停止实时订阅` are aligned in one row, and realtime start now requires the current service session to have a successfully prepared contract list. Existing historical contracts in SQLite are shown for operator context but no longer unlock subscription start by themselves. Verification passed with targeted API/quote-stream/WebUI/realtime-health tests at 45 tests.
- Contract manager separation completed: settings now has a standalone `合约管理器` panel between local API and realtime workers. `/api/contract-manager` exposes status plus start/stop controls; the manager maintains the local contract universe on its own refresh interval, heartbeat, and last-success state. Realtime subscription start now requires the contract manager to be healthy instead of relying on a one-shot contract-list pull. Verification passed with targeted API/quote-stream/WebUI/realtime-health tests at 45 tests, compileall, and agentic-SDLC check.
- Browser review follow-up WEBUI-015 is completed: overview/T型报价 no longer surface historical cached rows as if they were current realtime data. The overview underlying summary now uses the current realtime quote slice, sorts underlyings by exchange/product/delivery month, and T型报价 shows `未订阅` until the selected chain has fresh Quote rows from the current realtime subscription run.
- Realtime near-contract filtering follow-up WEBUI-016 is completed: `quote_stream.min_days_to_expiry` defaults to 1 and filters each product's candidate underlying contracts before selecting the configured nearest 1/2/3/all contract-month scope. Example: a 0-day `pt2606` is skipped, so default 2-contract scope selects `pt2608` and `pt2610`.
- WEBUI-016 verification passed: targeted quote-stream/API tests passed 31 tests; `uv run python -m pytest -s -q` passed 91 tests; compileall, local smoke, and agentic-SDLC check passed.
- Local server lifecycle script added at `scripts/odm-server.sh`. Use `scripts/odm-server.sh start|stop|restart|status` for WSL/local runs so realtime workers are stopped before WebUI restarts and stale SQLite holders are cleaned up.
- WEBUI-017 realtime subscription scope alignment completed: the minimum remaining-day filter now uses option expiry remaining days to exclude invalid near contracts such as `FG606` at 0 days even when future delivery is later, and the overview underlyings use the same per-product nearest valid contract scope as realtime Quote/Kline subscriptions. Verification passed with targeted quote-stream/WebUI/API tests at 44 tests, full `uv run pytest -s -q` at 94 tests, compileall, and agentic-SDLC check.
- WEBUI-018 unified realtime data outlet completed: realtime API/WebUI progress totals and overview rows now use the same SQLite-backed subscription scope. After clicking realtime start, all intended underlyings appear immediately as `待订阅`; partially refreshed chains show `订阅中`; chains whose current realtime Quote rows are complete show `已订阅`. Verification passed with targeted quote-stream/WebUI/API tests at 44 tests, full `uv run pytest -s -q` at 94 tests, compileall, and agentic-SDLC check.
- WEBUI-019 realtime start responsiveness completed: `/api/quote-stream/start` returns `starting` immediately while worker launch continues in the background, status endpoints use worker reports directly once available, and worker subscription-scope reads retry temporary SQLite locks instead of exiting. Verification passed with targeted quote-stream/WebUI/API tests at 44 tests, full `uv run pytest -s -q` at 94 tests, compileall, and agentic-SDLC check.
- WEBUI-020 enum-driven realtime state machine completed: quote-stream status is now persisted and returned as stable enums (`blocked`/`starting`/`running`/`stopped`), and WebUI controls/status cards no longer infer business state from Chinese message text. Verification passed with targeted quote-stream/WebUI/API tests at 45 tests, full `uv run pytest -s -q` at 95 tests, compileall, agentic-SDLC check, and a live local start that transitioned `starting` to `running` with 4 worker PIDs.
- WEBUI-021 overview lifecycle consistency completed after live browser feedback that progress could reach 100% while rows still looked un/subscribing. Root cause: progress counted created realtime Quote/Kline references, but overview row status counted fresh Quote writes after startup. The fix makes overview row status follow enum-backed subscription lifecycle, aligns scoped summary counts to worker progress, enables runtime SQLite WAL configuration for concurrent read/write, and keeps Quote/Kline coverage separate. Verification passed with targeted quote-stream/WebUI/API/WebUI-app tests at 47 tests, compileall, and live local evidence: realtime reached `11110/11110` Quote plus `11110/11110` Kline subscriptions; `/api/webui/overview?limit=140` returned 128 rows all `已订阅`, including `CZCE.FG607`/`CZCE.FG608` and `CZCE.MA607`/`CZCE.MA608`.
- WEBUI-022 T型报价实时刷新完成 after live browser feedback that the T型报价 timestamp stayed static even after Quote subscriptions were established. Root cause: Quote refs were ready, but visible Quote writes were effectively delayed behind the long Kline setup phase, and startup snapshots used one SQLite commit per symbol. Quote workers now bulk-write Quote snapshots, refresh Quote snapshots every 5 seconds during Kline setup, and avoid treating startup snapshots as metrics dirty signals. The realtime data source is backend-owned: WebUI/T型报价 requests only read the selected chain and do not drive subscription updates. Health checks also prefer the freshest actual DB quote write when worker `wait_update` statistics lag. Verification passed with targeted realtime tests including no-UI backend refresh coverage, full `uv run pytest -s -q` at 99 tests, compileall, agentic-SDLC check, and live local evidence: active night-session `SHFE.al2606` source time advanced from `2026-05-13 23:24:21` to `23:25:38`, while closed-session contracts such as `CZCE.CF607` correctly kept source time near `22:59:59`.
- WEBUI-023 running-phase realtime freshness follow-up completed after live browser feedback that T型报价 stopped again once all Kline subscriptions completed. Root cause: running workers wrote Quote rows only when `api.is_changing(...)` reported per-Quote field changes; in the live run `wait_update` continued but every worker reported `changed_quotes_written=0`, so DB writes stopped after the Kline setup force-refresh path ended. The fix adds a backend-owned running-phase current-Quote snapshot refresh, defaulting to every 1 second, so realtime data continues to update even when SDK change detection is quiet. Verification passed with `uv run pytest -s tests/test_quote_streamer.py -q` at 18 tests and `uv run python -m compileall -q src tests`.
- WEBUI-024 per-underlying subscription lifecycle display completed after operator feedback that the overview showed every row as `订阅中` while the global Kline phase was still running. Worker progress reports now include per-underlying Quote/Kline object counts, API/WebUI aggregate those counts across worker shards, and overview rows use each underlying contract's own counts so a chain turns `已订阅` as soon as its Quote plus Kline objects are complete. The status cell also shows subscribed/total objects for quick visual inspection. Verification passed with targeted quote-stream/API/WebUI read-model tests at 49 tests and compileall.

## Remaining Acceptance Work

- No open browser-review WebUI follow-up remains after WEBUI-024. Next acceptance work should be driven by a fresh browser review or live operator feedback.
