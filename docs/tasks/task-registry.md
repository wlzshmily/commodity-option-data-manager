# Task Registry

| Task ID | Status | Owner | Summary | Verification |
| --- | --- | --- | --- | --- |
| REL-001 | Done | Codex | Restore package/import/runtime metadata and CLI entrypoints | `uv run python -c "import option_data_manager"`; `uv run odm-collect --help` |
| REL-002 | Done | Codex | Initialize agentic-sdlc project governance | `scripts/agentic-sdlc/check-agentic-sdlc.ps1 -Root .` |
| REL-003 | Done | Codex | Consolidate local API, settings, API keys, and refresh endpoint | pytest API tests |
| REL-004 | Done | Codex | Preserve WebUI baseline and wire real settings/API information | WebUI TestClient smoke |
| REL-005 | Done | Codex | Add focused unit/API tests and sample database fixtures | `uv run pytest -q` |
| REL-006 | Done | Codex | Run bounded live TQSDK collection windows and tune batch thresholds | Background refresh worker; `odm-collect --max-batches` live reports; WebUI batch-progress read model; final tuned parallel full-market evidence |
| REL-007 | Done | Codex | Move local development target to WSL2 Ubuntu and align cloud Linux runtime setup | WSL setup script; Linux secret protector; pytest |
| REL-008 | Done | Codex | Add persistent local service logs and WebUI diagnostics for API/settings/refresh events | `python -m compileall -q src tests`; service log repository smoke |
| REL-009 | Done | Codex | Add repeatable local API/WebUI smoke command and rerun release-gate checks | `uv run pytest -q`; `uv run python scripts/smoke-local-app.py --database data/tmp-smoke/script-smoke.sqlite3`; server smoke checks |
| REL-010 | Done | Codex | Fix `odm-test-tqsdk` process environment credential resolution and record proxy-blocked live TQSDK attempt | `uv run pytest tests/test_tqsdk_cli.py -q`; transient-env `odm-test-tqsdk`; bounded `odm-collect` smoke attempt |
| REL-011 | Done | Codex | Tune full-market TQSDK subscription throughput and implement process-level shards plus realtime stream worker | Local perf evidence in ignored `docs/qa/live-evidence/perf-tuning/`; `odm-collect-parallel`; `odm-quote-stream`; `uv run pytest -q` |
| REL-012 | Done | Codex | Final readiness pass: complete full-market tuned coverage, verify WebUI/API display with real runtime data, and record signoff evidence | `odm-collect-parallel --until-complete` completed 864/864 batches; real DB WebUI/API smoke passed; `uv run pytest -q`; compileall; agentic-SDLC check |
| REL-013 | Done | Codex | Add WebUI/API controls for realtime stream workers | `/api/quote-stream` start/stop/status; WebUI settings controls; `uv run pytest -q` passed 41 tests; compileall; real runtime quote-stream control smoke |
| REL-014 | Done | Codex | Local acceptance UI/API polish: settings/overview table ergonomics, API Key copy/delete/full fingerprint, and TQSDK date-field API exposure | `uv run pytest tests/test_webui_read_model.py tests/test_api_app.py tests/test_quote_streamer.py -q`; compileall; local 8766 smoke |
| WEBUI-001 | Done | Codex | Fix T型报价 selectors, Chinese exchange labels, and remaining expiry days display | `uv run pytest -q` passed 46 tests; `uv run python -m compileall -q src tests`; agentic-SDLC check |
| WEBUI-002 | Done | Codex | Speed up T型报价 contract switching and fall back to stored TQSDK quote expiry fields | `uv run pytest -q` passed 47 tests; `uv run python -m compileall -q src tests`; agentic-SDLC check |
| WEBUI-003 | Done | Codex | Clarify realtime Quote status separately from batch collection progress on the overview page | `uv run pytest -q` passed 47 tests; `uv run python -m compileall -q src tests`; agentic-SDLC check |
| WEBUI-004 | Done | Codex | Add realtime subscription coverage progress and exchange tabs on the overview page | `uv run pytest -q` passed 47 tests; `uv run python -m compileall -q src tests`; agentic-SDLC check |
| WEBUI-005 | Done | Codex | Correct realtime progress semantics and stop persisted quote-stream worker process trees | `uv run pytest -q` passed 47 tests; `uv run python -m compileall -q src tests`; agentic-SDLC check |
| WEBUI-006 | Done | Codex | Track realtime subscription progress as Quote plus Kline objects, reset progress on fresh starts/stops, split Quote/Kline bars, and display duration/average/ETA timing | `uv run pytest -q` passed 50 tests; `uv run python -m compileall -q src tests`; agentic-SDLC check |
| WEBUI-007 | Done | Codex | Clarify overview batch catch-up versus realtime subscription status and calculate realtime ETA from worker cumulative progress and phase timestamps | WSL `smoke-local-app` passed; `python -m pytest tests/test_api_app.py tests/test_quote_streamer.py -s` passed 14 tests; compileall passed |
| WEBUI-008 | Done | Codex | Prioritize realtime subscriptions by nearest expiry month and mark the first N near-expiry months on the overview progress bar | WSL `uv run python -m pytest -s -q` passed 62 tests; live 4-worker restart used `--near-expiry-months 2`, reported near-month progress `6870/15218`, and rendered the near-month boundary marker |
| PERF-001 | Done | Codex | Add deterministic `odm-compare-iv-windows` evidence command and 3-day incremental K-line fetch plus 20-day cache splice for batch IV calculation | WSL `uv run python -m pytest -s -q` passed 57 tests; live direct-window IV sample max diff vs 20-day baseline `3.34e-12`; live cache-splice smoke requested `data_length=3` and completed with 0 errors |
| METRICS-001 | Done | Codex | Add realtime metrics dirty queue and independent metrics worker with default 30-second per-contract throttling and underlying-chain dirty marking | WSL `uv run python -m pytest -s -q` passed 61 tests; live API start/stop smoke started 1 quote worker plus 1 metrics worker and stopped both |
| METRICS-002 | Done | Codex | Restore realtime Kline object coverage while keeping IV/Greeks refresh on the nonblocking metrics worker cache-splice path | WSL `uv run python -m pytest -s -q` passed 61 tests; live 4-worker restart command used `--kline-data-length 3` without `--no-klines`, reported `kline_total=27386`, and Kline subscribed count advanced to 544 |
| KLINE-DEDUP-001 | Done | Codex | De-duplicate realtime Kline subscriptions and fetch each underlying Kline once per IV refresh chain batch before local OPTION_IMPV splicing | WSL full suite passed 66 tests; local probes showed wide Kline batches can block, so `--kline-batch-size` is experimental, defaults to stable single-symbol setup, and is now operator-configurable from settings |
| WEBUI-009 | Done | Codex | Add trading-session-aware realtime network health detection without holiday/post-close false alerts | WSL full suite passed 73 tests after correcting normal Friday-night handling |
| WEBUI-010 | Done | Codex | Add TQSDK official connection notification capture to realtime health reporting | WSL targeted realtime health/API/quote-stream tests passed 27 tests; full suite passed 75 tests |
| WEBUI-011 | Done | Codex | Bootstrap the active contract universe before realtime subscription startup on empty databases | WSL targeted API/quote-stream/health tests passed 28 tests; full suite passed 76 tests |
| CM-001 | Done | Codex | Implement mandatory dynamic contract management for realtime Quote/Kline workers | Startup discovery always runs; worker 0 periodic discovery; all workers incremental Quote/Kline reconciliation; WSL targeted API/quote-stream/health tests passed 29 tests; full suite passed 77 tests |
| OPS-001 | Done | Codex | Document Linux server deployment, release layout, systemd service, verification, public test exposure, upgrade, and rollback | `docs/operations/linux-server-deployment.md`; `bash scripts/agentic-sdlc/check-agentic-sdlc.sh` |
| WEBUI-012 | Done | Codex | Prevent realtime status/stop endpoints from returning 500 when SQLite service-state writes are temporarily locked | `uv run pytest -s -q` passed 79 tests; compileall; remote start/stop smoke returned 200 and stopped both workers |
| WEBUI-013 | Done | Codex | Keep realtime Quote workers alive when TQSDK batch Quote subscription times out by falling back to single-symbol subscriptions | `uv run pytest -s -q tests/test_quote_streamer.py`; `uv run pytest -s -q tests/test_api_app.py`; compileall; remote 20-symbol hotpatch smoke |
| WEBUI-014 | Done | Codex | Add realtime subscription contract month scope setting, defaulting to 2 near months with 1/2/3/all choices | WSL `uv run pytest -s -q` passed 82 tests; `uv run python -m compileall -q src tests`; `bash scripts/agentic-sdlc/check-agentic-sdlc.sh` |
| WEBUI-015 | Done | Codex | Correct WebUI display semantics for subscribed underlyings, underlying sort order, and T型报价 realtime freshness/status | WSL `uv run pytest -s -q` passed 90 tests; `uv run python -m compileall -q src tests`; `bash scripts/agentic-sdlc/check-agentic-sdlc.sh`; `uv run python scripts/smoke-local-app.py --database data/tmp-smoke/webui-015-smoke.sqlite3` |
| WEBUI-016 | Done | Codex | Filter near-contract subscription scope by minimum remaining days before selecting each product's nearest underlying contracts | `uv run python -m pytest -s -q tests/test_quote_streamer.py tests/test_api_app.py` passed 31 tests; `uv run python -m pytest -s -q` passed 91 tests; compileall, smoke, and agentic-SDLC check passed |
| OPS-002 | Done | Codex | Add local server lifecycle script that safely starts, stops, restarts, and inspects WebUI plus realtime worker processes | `bash -n scripts/odm-server.sh`; `scripts/odm-server.sh restart`; `/api/health` and `/api/settings` returned 200 |
| OPS-003 | Done | Codex | Update dedicated Ubuntu test server `39.103.49.231` to GitHub release `c54847bfe4c6` and keep `8765/tcp` publicly reachable | Server release prepared from GitHub codeload archive for commit `c54847bfe4c6d73c15d87e1a42a9eb74c0b0cd53`; remote `.venv/bin/python -m pytest -s -q` passed 127 tests; compileall, smoke-local-app, agentic-SDLC check, local HTTP smoke, and public `http://39.103.49.231:8765` smoke all passed |
| OPS-004 | Done | Codex | Update dedicated Ubuntu test server `39.103.49.231` to GitHub release `078061870956` and restore runtime workers | Server GitHub fetch timed out and codeload fallback succeeded for commit `0780618709561ccd864fd9b1401d313cf9b31ddb`; remote `.venv/bin/python -m pytest -s -q` passed 138 tests; compileall, smoke-local-app, agentic-SDLC check, local service smoke, public `http://39.103.49.231:8765` smoke, contract-manager restart, quote-stream restart, and metrics-worker restart all passed |
| OPS-005 | Done | Codex | Solidify the Ubuntu deployment safety process after the PP2606 current-slice gap finding | Added `docs/operations/deployment-log.md`; Linux deployment runbook now requires safe old-runtime quiescence, process/SQLite checks, runtime restart order, and data-readiness gates before deployment completion; release/risk/traceability/handoff records updated; `bash scripts/agentic-sdlc/check-agentic-sdlc.sh` |
| OPS-006 | Done | Codex | Add code-level operations monitoring for data-readiness and worker-health defects | `operations_monitor.py`; `/api/status` `operations`; WebUI overview alert banner; stale running batch read-model fields; metrics-worker status in quote-stream payload; `uv run python -m pytest -s -q` passed 141 tests; compileall and agentic-SDLC check passed |
| WEBUI-017 | Done | Codex | Align realtime subscription scope and overview underlyings to each product's nearest two valid option-expiry contracts | `uv run pytest -s -q tests/test_quote_streamer.py tests/test_webui_read_model.py tests/test_api_app.py` passed 44 tests; `uv run pytest -s -q` passed 94 tests; compileall; agentic-SDLC check |
| WEBUI-018 | Done | Codex | Make realtime overview use one SQLite-backed subscription scope with pending/subscribing/subscribed statuses | `uv run pytest -s -q tests/test_quote_streamer.py tests/test_webui_read_model.py tests/test_api_app.py` passed 44 tests; `uv run pytest -s -q` passed 94 tests; compileall; agentic-SDLC check |
| WEBUI-019 | Done | Codex | Keep realtime start/status responsive under SQLite locks and make start return asynchronously | `uv run pytest -s -q tests/test_quote_streamer.py tests/test_webui_read_model.py tests/test_api_app.py` passed 44 tests; `uv run pytest -s -q` passed 94 tests; compileall; agentic-SDLC check |
| WEBUI-020 | Done | Codex | Drive realtime quote-stream UI/business state from stable status enums instead of localized message text | `uv run pytest -s -q tests/test_quote_streamer.py tests/test_webui_read_model.py tests/test_api_app.py` passed 45 tests; `uv run pytest -s -q` passed 95 tests; compileall; agentic-SDLC check; live start transitioned `starting` to `running` with 4 worker PIDs |
| WEBUI-021 | Done | Codex | Align overview row status with realtime subscription lifecycle and keep runtime overview responsive | `uv run pytest -s -q tests/test_quote_streamer.py tests/test_webui_read_model.py tests/test_api_app.py tests/test_webui_app.py` passed 47 tests; compileall; live local overview returned 128 subscribed rows including `CZCE.FG607/FG608` and `CZCE.MA607/MA608` |
| WEBUI-022 | Done | Codex | Keep realtime data backend-independent from UI while realtime workers establish Kline subscriptions | Targeted realtime tests passed 29 tests including no-UI backend Quote refresh coverage; full `uv run pytest -s -q` passed 99 tests; compileall and agentic-SDLC check passed; live local active night-session `SHFE.al2606` advanced from `23:24:21` to `23:25:38`, while closed-session contracts such as `CZCE.CF607` correctly kept source time near `22:59:59` |
| MM-T001 | Done | Codex | Add moneyness classification helpers for CALL/PUT ITM/ATM/OTM, including nearest-strike/tie behavior | `tests/test_moneyness.py`; `tests/test_quote_streamer.py` |
| MM-T002 | Done | Codex | Add product/contract-level trading-session helper based on TQSDK Quote `trading_time` and current time/market time | `tests/test_trading_sessions.py`; `tests/test_webui_read_model.py` |
| MM-T003 | Done | Codex | Define aggregate progress/logging counters for moneyness recalculation, sticky Kline additions, and skip reasons | `tests/test_quote_streamer.py`; `tests/test_api_app.py` |
| MM-T004 | Done | Codex | Add settings/API/CLI/WebUI controls for moneyness filter and recalculation interval | `tests/test_api_app.py`; `tests/test_quote_streamer.py` |
| MM-T005 | Done | Codex | Keep Quote selection broad while filtering initial Kline selection by selected moneyness ranges | `tests/test_quote_streamer.py` |
| MM-T006 | Done | Codex | Add running-phase sticky Kline expansion on moneyness recalculation without intraday release | `tests/test_quote_streamer.py` |
| MM-T007 | Done | Codex | Surface current moneyness match count, cumulative sticky Kline count, skip reasons, and trading-session state in API/WebUI | `tests/test_api_app.py`; `tests/test_webui_read_model.py` |
| MM-T008 | Done | Codex | Complete verification, trial-run checklist, and SDLC record updates | `uv run pytest -s -q`; compileall; agentic-SDLC check |
| MM-T009 | Done | Codex | Correct live startup Kline target reporting so moneyness-filtered Klines are recomputed after the first Quote snapshot and initial subscription is not gated by trading session | `uv run pytest -s -q` passed 113 tests; compileall; local live restart reported Quote `11126/11126` and OTM Kline target `5009` |
| MM-T010 | Done | Codex | Add Kline subscription timeout watchdog and validate it in a live run so one blocking symbol cannot freeze the realtime worker | `uv run pytest -s -q tests/test_quote_streamer.py tests/test_api_app.py tests/test_webui_app.py tests/test_webui_read_model.py` passed 56 tests; compileall; live session showed advancing `current_kline_symbol` values with `kline_subscription_timeout_count=0` |
| MM-T011 | Done | Codex | Correct trading-session display and moneyness session gating to prefer fresh underlying Quote time over incomplete theoretical `trading_time` night profiles | `uv run pytest -s -q tests/test_trading_sessions.py tests/test_webui_read_model.py tests/test_quote_streamer.py` passed 45 tests; compileall; live API showed fresh SHFE ad/ag/al Quote rows as `交易中` and stale/non-night AP as `休市` |
| MM-T012 | Done | Codex | Distinguish pending subscription from subscribed no-Quote-time session state in the realtime UI | `uv run pytest -s -q tests/test_webui_read_model.py tests/test_trading_sessions.py tests/test_quote_streamer.py` passed 48 tests; pending rows report `待行情`, while `已订阅` rows without underlying Quote time report `休市` |
| MM-T013 | Done | Codex | Mask T型报价 historical values only until the selected chain's Quote objects are subscribed, then display the current cached slice even while K线 catches up or the market is closed | `uv run pytest -s -q tests/test_webui_read_model.py tests/test_trading_sessions.py tests/test_quote_streamer.py` passed 48 tests; compileall; diff check |
| MM-T014 | Done | Codex | Mask T型报价 option legs outside the active moneyness Kline filter during realtime operation | `uv run pytest -s -q tests/test_webui_read_model.py tests/test_webui_app.py tests/test_trading_sessions.py tests/test_moneyness.py tests/test_quote_streamer.py` passed 53 tests; compileall; diff check; agentic-SDLC check |
| MM-T015 | Done | Codex | Prevent Kline setup from rewriting stale Quote snapshots and starving underlying Quote freshness | `uv run pytest -s -q tests/test_quote_streamer.py tests/test_webui_read_model.py tests/test_webui_app.py tests/test_api_app.py` passed 62 tests; compileall; diff check; agentic-SDLC check; live restart verified `DCE.b2607` Quote recovered to current market time |
| MM-T016 | Done | Codex | Calculate T型报价 IV/Greeks coverage against subscribed/displayable option legs only | `uv run pytest -s -q tests/test_webui_app.py tests/test_webui_read_model.py tests/test_quote_streamer.py tests/test_api_app.py` passed 63 tests; compileall; diff check; agentic-SDLC check |
| MM-T017 | Done | Codex | Correct WebUI product Chinese names by preferring TQSDK Quote `instrument_name` and fixing `ad` fallback to 铝合金 | `uv run pytest -s -q tests/test_webui_read_model.py tests/test_webui_app.py tests/test_quote_streamer.py tests/test_api_app.py` passed 65 tests; compileall; diff check |
| MM-T018 | Done | Codex | Use robust TQSDK Quote reference-price fallback for moneyness Kline filtering when `last_price` is empty | Targeted moneyness/WebUI/API suite passed 73 tests; full `uv run python -m pytest -s -q` passed 132 tests after tuple-row, report-dir isolation, Saturday-night session-gating, and realtime progress target/subscribed口径 fixes; compileall and agentic-SDLC check passed; live local realtime reached Quote `11126/11126`, Kline `5496/5496`, AP610 `36/36`, 0 Kline errors |
| MM-T019 | Done | Codex | Centralize option-chain moneyness classification so realtime Kline filtering and T型报价 coloring use one backend result | Targeted `tests/test_moneyness.py`, T型报价 read-model/app, and quote-stream moneyness tests passed; AP610 local SQLite verification showed reference `7650`, ATM strike `7700`, CALL OTM `24`, PUT OTM `12`, and Kline target `37` including the underlying future |
| MM-T020 | Done | Codex | Show overview CALL/PUT counts from realtime Kline moneyness targets while subscriptions are running | Full `uv run python -m pytest -s -q` passed 137 tests; compileall and agentic-SDLC check passed; AP610 overview now uses target CALL `24` and PUT `12` while status keeps Kline total `37` including the underlying future |
| WEBUI-025 | Done | Codex | Clarify overview subscription row status and split row-level Quote/Kline progress display | `uv run python -m pytest -s -q tests/test_webui_read_model.py tests/test_webui_app.py tests/test_api_app.py tests/test_quote_streamer.py` passed 68 tests; full `uv run python -m pytest -s -q` passed 128 tests; compileall and agentic-SDLC check passed |
