# Agent Handoffs

## 2026-05-08

- Active work: implement approved release plan.
- Important decisions: five commodity exchanges are in scope; WebUI baseline is existing source; API Key enforcement is optional by default.
- Safety note: never expose real TQSDK credentials or full API keys in reports, logs, screenshots, or fixtures.

## 2026-05-09

- Active work: CR-2026-05-09-LOGS implemented to improve local observability.
- Delivered: persistent `service_logs` table, `/api/logs`, WebUI 日志与诊断页, and safe event logging for settings/API/security/background refresh flows.
- Verification: `python -m compileall -q src tests` and service-log repository smoke passed; full `uv run pytest -q` could not recreate dependencies because PyPI scipy download failed through the tunnel.
- Safety note: service-log contexts intentionally record booleans, setting keys, event categories, and key metadata only; do not add raw TQSDK passwords or full API keys.
- Follow-up verification: dependency recovery succeeded; `uv run pytest -q` now passes 30 tests, API/WebUI factory smoke passes via `scripts/smoke-local-app.py`, and bound localhost server smoke passed for `odm-api`/`odm-webui`.
- Remaining blocker: live full-market completion still requires configured local TQSDK credentials and a completed evidence window.
- TQSDK follow-up: transient credentials were supplied only through environment variables; `odm-test-tqsdk` env resolution bug was fixed, then both connection check and bounded collection reached outbound proxy denial to `auth.shinnytech.com` before authentication could complete.
- Local tuning follow-up: Windows local TQSDK login passed with encrypted SQLite credentials. Short-window collector `wait_update` deadline was corrected to `time.time() + 1` and covered by unit test. Perf evidence under ignored `docs/qa/live-evidence/perf-tuning/` favors `option_batch_size=40`, `wait_cycles=1`; quote-only realtime shards are much cheaper than full K-line/metrics refresh; 2-4 independent TQSDK worker processes improve throughput with sublinear scaling. `uv run pytest -q` passes 31 tests.
- Implementation follow-up: `odm-collect-parallel` now runs disjoint underlying ranges across independent TQSDK API processes and per-worker collection scopes. `odm-quote-stream` now runs bounded realtime subscription shards for UI freshness. Both commands write secret-safe reports only.
- Final acceptance follow-up: local direct TQSDK full-market catch-up completed through `odm-collect-parallel` with 4 workers, 6 waves, 864/864 batches successful, 0 failed, 27,386 active options, 565,000 K-line rows, and 27,386 metrics rows. Evidence is under ignored `docs/qa/live-evidence/final-parallel-catchup/summary.json`; no credential values were written.
- Final verification follow-up: `uv run pytest -q` passed 41 tests, compileall passed, `scripts\agentic-sdlc\check-agentic-sdlc.ps1 -Root .` passed, and real runtime API/WebUI TestClient smoke passed with collection completion `1.0`.
- Quote stream controls follow-up: `/api/quote-stream` status/start/stop and WebUI settings controls are implemented. Tests verify workers start/stop through subprocess orchestration and that passwords are not passed on the command line. Live control smoke started and stopped one 10-symbol worker and left no running worker process.
- Local acceptance UI/API polish follow-up: settings and overview page review issues were addressed locally, including message tone classes, disabled worker controls, fixed table widths, Chinese product labels, API Key copy/delete/full fingerprint display, and no-message hidden states. TQSDK same-name date fields (`expire_datetime`, `last_exercise_datetime`, `delivery_year/month`, `exercise_year/month`) are now persisted/backfilled from quote subscriptions where available and exposed through API responses; derived remaining-day fields are explicitly named as derived. Current local DB rows may show `null` until a real quote subscription provides those TQSDK fields.
- Completed WEBUI-001 / CR-2026-05-09-WEBUI-TQUOTE.
- T型报价 selector changes now choose the first valid row for changed exchange/product and the exact row for changed expiry, then reload the quote chain.
- Remaining expiry days are derived from saved option `expire_datetime` values in the WebUI read model and shown in the overview table and T型报价 toolbar.
- Verification passed: `uv run pytest -q` passed 46 tests; `uv run python -m compileall -q src tests`; `scripts\agentic-sdlc\check-agentic-sdlc.ps1 -Root .`.
- WEBUI-002 follow-up: T型报价 contract switches now fetch only the selected quote chain and do not re-aggregate selectors. Latest K-line lookup is scoped to the selected chain, and remaining expiry days fall back to TQSDK `expire_datetime`/`expire_rest_days` values from `quote_current.raw_payload_json` when `instruments` fields are null. Verification passed: `uv run pytest -q` passed 47 tests; compileall; agentic-SDLC check.
- WEBUI-003 follow-up: overview progress cards now distinguish realtime Quote workers from batch collection progress, showing realtime worker count/PIDs and a separate latest data write time while labeling batch-plan update time as batch-only. Verification passed: `uv run pytest -q` passed 47 tests; compileall; agentic-SDLC check.
- WEBUI-004 follow-up: overview now shows realtime subscription coverage as a progress bar using active quote symbols with Quote data, and the underlying summary table is split by exchange tabs while retaining the existing issue filter. WebUI/API quote-stream status also detects persisted live worker PIDs after a local service restart. Verification passed: `uv run pytest -q` passed 47 tests; compileall; agentic-SDLC check.
- WEBUI-005 follow-up: realtime subscription progress now uses quote-row write coverage, with market-field coverage shown separately as a source-quality metric. Quote-stream stop checks persisted worker PIDs by command line and terminates process trees, preventing stale PID false-running states. Verification passed: `uv run pytest -q` passed 47 tests; compileall; agentic-SDLC check.
- WEBUI-006 follow-up: realtime subscription progress now comes from worker runtime reports and is calculated as `(subscribed Quote objects + subscribed Kline objects) / (required Quote objects + required Kline objects)`. `odm-quote-stream` keeps Kline subscription references alive alongside Quote references, writes progress reports while subscribing, clears stale reports on fresh starts, and reports zero progress when stopped so restarts begin from zero. The overview progress card also shows current stage, separate Quote/Kline bars, total subscription elapsed time, average seconds per subscribed object, and estimated remaining time. Verification passed: `uv run pytest -q` passed 50 tests; compileall; agentic-SDLC check.
- WEBUI-007 acceptance follow-up: overview progress wording now separates realtime subscription from batch catch-up. The old `后台任务` card is now `批量补采`, with copy explaining that realtime subscription is independent. Realtime subscription ETA is deterministic code, not Agent logic: quote-stream workers report cumulative subscribed counts plus Quote/Kline phase timestamps; API computes phase speed and total remaining time from those reports; the browser only counts down between backend reports. WSL smoke and targeted API/quote-stream tests passed.
- PERF-001 follow-up: Added deterministic `odm-compare-iv-windows` CLI for live TQSDK evidence comparing latest `OPTION_IMPV` across daily K-line `data_length` values. WSL sample on 6 AP610 options had 24/24 successful points and max absolute IV difference vs 20-day baseline about `3.34e-12`. Batch collection now checks the local `kline_20d_current` cache: when both option and underlying have at least 20 rows, it requests 3 daily bars from TQSDK, merges by `bar_datetime`, clips to the latest 20 rows, builds a TQSDK-like multi-symbol frame including `duration`, and passes that to `OPTION_IMPV`; otherwise it fetches 20 bars. Live cache-splice smoke on `CZCE.AP611C8400` observed `data_length=3`, completed with 0 errors, and wrote finite IV. Realtime `odm-quote-stream` remains at 20 daily bars by default and still exposes `--kline-data-length` for controlled experiments.
- METRICS-001 follow-up: Realtime Quote workers now enqueue IV/Greeks refresh work in `metrics_dirty_queue` instead of computing metrics synchronously. Effective option price changes enqueue the option; effective underlying future price changes enqueue its active option chain through a chain-level interval limiter. `odm-metrics-worker` runs as a separate process when `/api/quote-stream/start` starts realtime workers, with default `metrics.min_interval_seconds=30` and configurable `metrics.underlying_chain_interval_seconds=30`. Failed or source-empty metrics attempts are retried later and do not overwrite prior finite `option_source_metrics_current` values. WSL full suite passed 61 tests; live API smoke started and stopped one quote worker plus one metrics worker.
- METRICS-002 correction: The Quote-only startup attempt was rejected after review because daily K-lines update during the trading session and must keep realtime object coverage. API-started realtime workers no longer pass `--no-klines`; they subscribe Quote plus Kline objects and pass configurable `--kline-data-length` (default 3) to reduce each serial's history window without changing object coverage. Metrics worker still performs IV refresh through the 3-day fresh fetch plus local 20-day cache splice, with 20-day fallback when cache is incomplete.
- METRICS-002 verification: WSL targeted API/quote-stream tests passed 15 tests; WSL full suite passed 61 tests; compileall and agentic-SDLC check passed. Live 4-worker restart verified command args `--kline-data-length 3` with no `--no-klines`, `/api/quote-stream` reported `kline_total=27386`, and Kline subscribed count advanced from 272 to 544 while Quote was complete at 27,766/27,766.
- WEBUI-008 follow-up: Realtime quote/kline subscription order now defaults to nearest-expiry-month priority. Settings expose `quote_stream.prioritize_near_expiry` and `quote_stream.near_expiry_months` (default 2). Workers report near-expiry subscribed/total counts, API/WebUI aggregate them, and the overview total progress bar draws a red `近N月` boundary marker at the first N expiry-month object boundary. WSL targeted API/quote-stream tests passed 16 tests; WSL full suite passed 62 tests; live 4-worker restart used `--near-expiry-months 2` and reported near-month progress advancing to `6870/15218`.
- KLINE-DEDUP correction: Browser review found realtime Kline pair subscriptions repeated the same underlying daily K-line once per option contract. `odm-quote-stream` now subscribes de-duplicated single-symbol Klines: each option symbol once and each referenced underlying future once. Batch/metrics IV refresh now fetches option Klines individually, fetches the underlying Kline once per chain batch, then splices cached local option+underlying records into the two-symbol frame required by TQSDK `OPTION_IMPV`. `odm-metrics-worker` groups dirty option tasks by underlying before refreshing so one underlying Kline fetch can serve the chain slice. WSL targeted tests passed: `pytest -s tests/test_quote_streamer.py tests/test_chain_collector.py tests/test_metrics_worker.py -q` (13 passed) and `pytest -s tests/test_api_app.py tests/test_webui_read_model.py -q` (17 passed).
- KLINE-DEDUP performance clarification: The de-dup change improves correctness/resource duplication for local cache and IV refresh, but it does not reduce realtime Kline serial setup call count enough by itself. A local probe found wide multi-symbol Kline serial batches may block on some symbol combinations, so `--kline-batch-size` is available for controlled experiments but defaults to stable single-symbol setup. WSL full suite passed 66 tests after the clarification.
- KLINE-BATCH setting follow-up: Settings/API/WebUI now expose `quote_stream.kline_batch_size` with default `1`; `/api/quote-stream/start` passes it through as `--kline-batch-size`, so operators can test `2` without changing code. This remains an operator-controlled experiment rather than the default because prior probes showed some multi-symbol combinations can stall.
- WEBUI-009 follow-up: realtime health detection now distinguishes subscription setup, closed sessions, opening grace, holiday/no-market-evidence quiet state, stale worker heartbeat, stale TQSDK `wait_update`, and stale Quote writes. The overview shows an `开盘网络` card, and settings status includes the health label. Detection is intentionally conservative: it only raises network warnings during broad commodity trading-session windows after today/current-session futures Quote source time has advanced.
- WEBUI-009 correction: do not use "tomorrow is trading" to gate night sessions because normal Friday night should remain possible before a weekend. The checker now allows Monday-Friday night windows and Saturday early morning continuation, then suppresses only when a statutory holiday is found ahead; normal weekends do not suppress Friday night.
- WEBUI-010 follow-up: realtime workers now attach TQSDK `TqNotify` and record official network notification codes/content into worker progress reports. API/WebUI aggregate `tqsdk_connection_status`, latest notify content, disconnect time, and restore time; the `开盘网络` health result prefers fresh SDK `disconnected`/`reconnecting` notifications during confirmed trading sessions. This is observability only: it relies on TQSDK's built-in reconnect/resend behavior and does not force full resubscription. Verification passed: targeted realtime health/API/quote-stream tests passed 27 tests; WSL full suite passed 75 tests.
- WEBUI-011 correction: realtime subscription startup now owns the empty-database bootstrap case. `/api/quote-stream/start` checks active option count first; if the universe is empty, it creates a temporary TQSDK API, runs one `_discover_and_persist_market` pass in the parent API process, closes that API, and only then starts realtime Quote/Kline workers with `--no-discover`. This keeps worker boundaries clean while preventing new deployments from failing with "no active quote symbols". If discovery fails or returns zero active options, the API returns blocked with a clear message. Verification passed: targeted API/quote-stream/health tests passed 28 tests; WSL full suite passed 76 tests.
- CM-001 correction: dynamic contract management is now an actual realtime behavior, not only a draft spec. Startup discovery is mandatory on every realtime start. During long-lived realtime operation, worker 0 periodically refreshes the TQSDK contract universe into SQLite and all workers periodically reconcile their shard's in-memory Quote/Kline maps against active symbols. Reconciliation keeps unchanged references, removes inactive symbols from local maps, and subscribes only newly active symbols. Tests cover adding a new strike and inactivating an old option without full rebuild. Verification passed: targeted API/quote-stream/health tests passed 29 tests; WSL full suite passed 77 tests.
- Dynamic contract manager clarification: user confirmed that active contract universe changes can include new futures underlyings with option chains and new strike-price contracts on existing chains after underlying price movement. Additions and removals are determined before the next session opens: before night session when available, otherwise before the next morning session, including holiday edge cases. Mid-session contract add/remove is not expected. The v0.1 realtime baseline now implements mandatory startup refresh plus periodic runtime refresh/reconciliation; exact exchange/product-specific pre-session scheduling remains a future precision refinement.
