# Agent Handoffs

## 2026-05-17

- OPS-004 deployment completed for the dedicated Ubuntu test server `39.103.49.231`.
- Target release: GitHub commit `0780618709561ccd864fd9b1401d313cf9b31ddb`, deployed at `/opt/option-data-manager/releases/078061870956`; `/opt/option-data-manager/current` points there and `/opt/option-data-manager/current-release.txt` contains the full SHA.
- Server-side `git fetch` timed out connecting to GitHub, but the server successfully downloaded the official GitHub codeload archive for the same commit and rebuilt the release from that archive.
- First deployment candidate `17f289d638f1` was not activated because remote validation found a request-metric SQLite commit-state exception (`cannot commit - no transaction is active`). Commit `078061870956` hardened request metric retry handling and added regression coverage in `tests/test_api_app.py`.
- Remote verification passed: `.venv/bin/python -m pytest -s -q` (138 passed), compileall, `scripts/smoke-local-app.py`, and `bash scripts/agentic-sdlc/check-agentic-sdlc.sh`.
- Runtime verification passed: `odm-webui.service` active under systemd as user `odm`, listening on `0.0.0.0:8765`; local server smoke and external public smoke against `http://39.103.49.231:8765` both returned 200 for core UI/API/static endpoints.
- Runtime workers were restored after the service restart: contract manager is healthy with 28,060 active options, quote-stream worker PID `60222` and metrics-worker PID `60223` run from the new release, and observed realtime progress reached Quote `3200/11126`, Kline `0/5077`, OTM moneyness target `5077`, with 0 Kline subscription errors.
- No real TQSDK password, raw API key, or live credential evidence was written into tracked files.

- MM-T019 completed locally in WSL2 Ubuntu. The realtime Kline moneyness filter and T型报价 display now consume a shared backend classification context from `moneyness.py`; JavaScript no longer re-derives CALL/PUT moneyness from price, and instead maps backend `moneyness` plus row `is_atm` to display classes.
- Code areas touched: `src/option_data_manager/moneyness.py`, `src/option_data_manager/quote_streamer.py`, `src/option_data_manager/webui/read_model.py`, `src/option_data_manager/webui/app.py`, `tests/test_moneyness.py`, `tests/test_webui_read_model.py`, and `tests/test_webui_app.py`.
- Verification so far: targeted `uv run python -m pytest -s -q tests/test_moneyness.py tests/test_webui_read_model.py::test_tquote_masks_rows_outside_realtime_moneyness_scope tests/test_webui_app.py::test_tquote_coloring_uses_backend_moneyness_classification tests/test_quote_streamer.py::test_moneyness_filter_keeps_quote_scope_broad_but_filters_klines tests/test_quote_streamer.py::test_moneyness_filter_uses_underlying_quote_price_fallbacks` passed 11 tests; local AP610 SQLite verification showed reference price `7650`, ATM strike `7700`, CALL OTM `24`, PUT OTM `12`, and Kline target `37` including the underlying future.
- MM-T020 completed locally in WSL2 Ubuntu. Overview rows now display realtime Kline target CALL/PUT side counts when moneyness-filtered subscriptions are running, instead of the full option-chain CALL/PUT counts. Worker progress and API aggregation expose `kline_call_total`, `kline_put_total`, and subscribed counterparts per underlying; WebUI keeps a fallback to full-chain counts if reading old reports without side fields.
- Verification passed: targeted quote-stream/WebUI tests, full `uv run python -m pytest -s -q` (137 passed), compileall, and `bash scripts/agentic-sdlc/check-agentic-sdlc.sh`.

## 2026-05-16

- WEBUI-025 completed locally in WSL2 Ubuntu. The overview row status no longer treats Quote-complete rows with `kline_total=0` as fully `已订阅`; it keeps the row at `订阅中` until both Quote and Kline are complete and displays `Quote x/y` plus `K线 x/y` separately. Running-phase moneyness recalculation now only expands row/global Kline target totals when actual eligible Kline subscriptions are added, avoiding closed-session target growth.
- Code areas touched: `src/option_data_manager/quote_streamer.py`, `src/option_data_manager/webui/read_model.py`, `src/option_data_manager/webui/app.py`, `tests/test_quote_streamer.py`, and `tests/test_webui_read_model.py`.
- Verification passed: targeted `uv run python -m pytest -s -q tests/test_webui_read_model.py tests/test_webui_app.py tests/test_api_app.py tests/test_quote_streamer.py` (68 passed), full `uv run python -m pytest -s -q` (128 passed), compileall, and `bash scripts/agentic-sdlc/check-agentic-sdlc.sh`.

- OPS-003 deployment completed for the dedicated Ubuntu test server `39.103.49.231`.
- Target release: GitHub commit `c54847bfe4c6d73c15d87e1a42a9eb74c0b0cd53`, deployed at `/opt/option-data-manager/releases/c54847bfe4c6`; `/opt/option-data-manager/current` points there and `/opt/option-data-manager/current-release.txt` contains the full SHA.
- Server-side `git fetch` from GitHub twice failed with `GnuTLS recv error (-110)`, but the server successfully downloaded the official GitHub codeload archive for the same commit and rebuilt the release from that archive.
- Remote verification passed: `.venv/bin/python -m pytest -s -q` (127 passed), compileall, `scripts/smoke-local-app.py`, and `bash scripts/agentic-sdlc/check-agentic-sdlc.sh`.
- Runtime verification passed: `odm-webui.service` active under systemd as user `odm`, listening on `0.0.0.0:8765`; local server smoke and external public smoke against `http://39.103.49.231:8765` both returned 200 for core UI/API/static endpoints.
- No real TQSDK password, raw API key, or live credential evidence was written into tracked files.

- API verification check completed with no business-code changes.
- Current workspace verification passed: `uv run python -m pytest -s -q` (125 passed), compileall, agentic-SDLC shell check, TestClient smoke, and a temporary bound API server smoke on `127.0.0.1:18770`.
- The first `uv run pytest -q` attempt failed before running tests due to pytest capture cleanup `FileNotFoundError`; the no-capture rerun passed and should be preferred on this WSL `/mnt/c` workspace if the capture issue recurs.
- Subscription distinction capability exists today through `/api/quote-stream` `progress.underlying_progress` and WebUI read-model status fields. There is no dedicated narrow REST endpoint like `/api/subscriptions/{underlying}` yet; callers currently read aggregate progress or WebUI overview/T型报价 payloads.
- API page redesign follow-up completed after browser feedback. `/#api` is now a status and access page rather than a placeholder: it shows Base URL, Swagger/OpenAPI links, auth guidance, common endpoints, lightweight runtime summary, and a jump to API Key management in settings. A lightweight `/api/webui/api-summary` endpoint was added because `/api/status` performs heavier overview aggregation and can be slow on the large local SQLite database. Verification passed with targeted WebUI/API tests, compileall, local smoke, agentic-SDLC check, and live `8765` HTTP checks.

## 2026-05-08

- Active work: implement approved release plan.
- Important decisions: five commodity exchanges are in scope; WebUI baseline is existing source; API Key enforcement is optional by default.
- Safety note: never expose real TQSDK credentials or full API keys in reports, logs, screenshots, or fixtures.

## 2026-05-09

- Active work: CR-2026-05-09-LOGS implemented to improve local observability.
- Delivered: persistent `service_logs` table, `/api/logs`, WebUI 日志与诊断页, and safe event logging for settings/API/security/background refresh flows.
- Verification: `python -m compileall -q src tests` and service-log repository smoke passed; full `uv run pytest -q` could not recreate dependencies because PyPI scipy download failed through the tunnel.
- Safety note: service-log contexts intentionally record booleans, setting keys, event categories, and key metadata only; do not add raw TQSDK passwords or full API keys.
- Follow-up verification: dependency recovery succeeded; `uv run pytest -q` now passes 30 tests, API/WebUI factory smoke passes via `scripts/smoke-local-app.py`, and bound localhost server smoke passed for `odm-api`/`odm-webui`.
- Remaining blocker: live full-market completion still requires configured local TQSDK credentials and a completed evidence window.
- TQSDK follow-up: transient credentials were supplied only through environment variables; `odm-test-tqsdk` env resolution bug was fixed, then both connection check and bounded collection reached outbound proxy denial to `auth.shinnytech.com` before authentication could complete.
- Local tuning follow-up: Windows local TQSDK login passed with encrypted SQLite credentials. Short-window collector `wait_update` deadline was corrected to `time.time() + 1` and covered by unit test. Perf evidence under ignored `docs/qa/live-evidence/perf-tuning/` favors `option_batch_size=40`, `wait_cycles=1`; quote-only realtime shards are much cheaper than full K-line/metrics refresh; 2-4 independent TQSDK worker processes improve throughput with sublinear scaling. `uv run pytest -q` passes 31 tests.
- Implementation follow-up: `odm-collect-parallel` now runs disjoint underlying ranges across independent TQSDK API processes and per-worker collection scopes. `odm-quote-stream` now runs bounded realtime subscription shards for UI freshness. Both commands write secret-safe reports only.
- Final acceptance follow-up: local direct TQSDK full-market catch-up completed through `odm-collect-parallel` with 4 workers, 6 waves, 864/864 batches successful, 0 failed, 27,386 active options, 565,000 K-line rows, and 27,386 metrics rows. Evidence is under ignored `docs/qa/live-evidence/final-parallel-catchup/summary.json`; no credential values were written.
- Final verification follow-up: `uv run pytest -q` passed 41 tests, compileall passed, `scripts\agentic-sdlc\check-agentic-sdlc.ps1 -Root .` passed, and real runtime API/WebUI TestClient smoke passed with collection completion `1.0`.
- Quote stream controls follow-up: `/api/quote-stream` status/start/stop and WebUI settings controls are implemented. Tests verify workers start/stop through subprocess orchestration and that passwords are not passed on the command line. Live control smoke started and stopped one 10-symbol worker and left no running worker process.
- Local acceptance UI/API polish follow-up: settings and overview page review issues were addressed locally, including message tone classes, disabled worker controls, fixed table widths, Chinese product labels, API Key copy/delete/full fingerprint display, and no-message hidden states. TQSDK same-name date fields (`expire_datetime`, `last_exercise_datetime`, `delivery_year/month`, `exercise_year/month`) are now persisted/backfilled from quote subscriptions where available and exposed through API responses; derived remaining-day fields are explicitly named as derived. Current local DB rows may show `null` until a real quote subscription provides those TQSDK fields.
- Completed WEBUI-001 / CR-2026-05-09-WEBUI-TQUOTE.
- T型报价 selector changes now choose the first valid row for changed exchange/product and the exact row for changed expiry, then reload the quote chain.
- Remaining expiry days are derived from saved option `expire_datetime` values in the WebUI read model and shown in the overview table and T型报价 toolbar.
- Verification passed: `uv run pytest -q` passed 46 tests; `uv run python -m compileall -q src tests`; `scripts\agentic-sdlc\check-agentic-sdlc.ps1 -Root .`.
- WEBUI-002 follow-up: T型报价 contract switches now fetch only the selected quote chain and do not re-aggregate selectors. Latest K-line lookup is scoped to the selected chain, and remaining expiry days fall back to TQSDK `expire_datetime`/`expire_rest_days` values from `quote_current.raw_payload_json` when `instruments` fields are null. Verification passed: `uv run pytest -q` passed 47 tests; compileall; agentic-SDLC check.
- WEBUI-003 follow-up: overview progress cards now distinguish realtime Quote workers from batch collection progress, showing realtime worker count/PIDs and a separate latest data write time while labeling batch-plan update time as batch-only. Verification passed: `uv run pytest -q` passed 47 tests; compileall; agentic-SDLC check.
- WEBUI-004 follow-up: overview now shows realtime subscription coverage as a progress bar using active quote symbols with Quote data, and the underlying summary table is split by exchange tabs while retaining the existing issue filter. WebUI/API quote-stream status also detects persisted live worker PIDs after a local service restart. Verification passed: `uv run pytest -q` passed 47 tests; compileall; agentic-SDLC check.
- WEBUI-005 follow-up: realtime subscription progress now uses quote-row write coverage, with market-field coverage shown separately as a source-quality metric. Quote-stream stop checks persisted worker PIDs by command line and terminates process trees, preventing stale PID false-running states. Verification passed: `uv run pytest -q` passed 47 tests; compileall; agentic-SDLC check.
- WEBUI-006 follow-up: realtime subscription progress now comes from worker runtime reports and is calculated as `(subscribed Quote objects + subscribed Kline objects) / (required Quote objects + required Kline objects)`. `odm-quote-stream` keeps Kline subscription references alive alongside Quote references, writes progress reports while subscribing, clears stale reports on fresh starts, and reports zero progress when stopped so restarts begin from zero. The overview progress card also shows current stage, separate Quote/Kline bars, total subscription elapsed time, average seconds per subscribed object, and estimated remaining time. Verification passed: `uv run pytest -q` passed 50 tests; compileall; agentic-SDLC check.
- WEBUI-007 acceptance follow-up: overview progress wording now separates realtime subscription from batch catch-up. The old `后台任务` card is now `批量补采`, with copy explaining that realtime subscription is independent. Realtime subscription ETA is deterministic code, not Agent logic: quote-stream workers report cumulative subscribed counts plus Quote/Kline phase timestamps; API computes phase speed and total remaining time from those reports; the browser only counts down between backend reports. WSL smoke and targeted API/quote-stream tests passed.
- PERF-001 follow-up: Added deterministic `odm-compare-iv-windows` CLI for live TQSDK evidence comparing latest `OPTION_IMPV` across daily K-line `data_length` values. WSL sample on 6 AP610 options had 24/24 successful points and max absolute IV difference vs 20-day baseline about `3.34e-12`. Batch collection now checks the local `kline_20d_current` cache: when both option and underlying have at least 20 rows, it requests 3 daily bars from TQSDK, merges by `bar_datetime`, clips to the latest 20 rows, builds a TQSDK-like multi-symbol frame including `duration`, and passes that to `OPTION_IMPV`; otherwise it fetches 20 bars. Live cache-splice smoke on `CZCE.AP611C8400` observed `data_length=3`, completed with 0 errors, and wrote finite IV. Realtime `odm-quote-stream` remains at 20 daily bars by default and still exposes `--kline-data-length` for controlled experiments.
- METRICS-001 follow-up: Realtime Quote workers now enqueue IV/Greeks refresh work in `metrics_dirty_queue` instead of computing metrics synchronously. Effective option price changes enqueue the option; effective underlying future price changes enqueue its active option chain through a chain-level interval limiter. `odm-metrics-worker` runs as a separate process when `/api/quote-stream/start` starts realtime workers, with default `metrics.min_interval_seconds=30` and configurable `metrics.underlying_chain_interval_seconds=30`. Failed or source-empty metrics attempts are retried later and do not overwrite prior finite `option_source_metrics_current` values. WSL full suite passed 61 tests; live API smoke started and stopped one quote worker plus one metrics worker.
- METRICS-002 correction: The Quote-only startup attempt was rejected after review because daily K-lines update during the trading session and must keep realtime object coverage. API-started realtime workers no longer pass `--no-klines`; they subscribe Quote plus Kline objects and pass configurable `--kline-data-length` (default 3) to reduce each serial's history window without changing object coverage. Metrics worker still performs IV refresh through the 3-day fresh fetch plus local 20-day cache splice, with 20-day fallback when cache is incomplete.
- METRICS-002 verification: WSL targeted API/quote-stream tests passed 15 tests; WSL full suite passed 61 tests; compileall and agentic-SDLC check passed. Live 4-worker restart verified command args `--kline-data-length 3` with no `--no-klines`, `/api/quote-stream` reported `kline_total=27386`, and Kline subscribed count advanced from 272 to 544 while Quote was complete at 27,766/27,766.
- WEBUI-008 follow-up: Realtime quote/kline subscription order now defaults to nearest-expiry-month priority. Settings expose `quote_stream.prioritize_near_expiry` and `quote_stream.near_expiry_months` (default 2). Workers report near-expiry subscribed/total counts, API/WebUI aggregate them, and the overview total progress bar draws a red `近N月` boundary marker at the first N expiry-month object boundary. WSL targeted API/quote-stream tests passed 16 tests; WSL full suite passed 62 tests; live 4-worker restart used `--near-expiry-months 2` and reported near-month progress advancing to `6870/15218`.
- KLINE-DEDUP correction: Browser review found realtime Kline pair subscriptions repeated the same underlying daily K-line once per option contract. `odm-quote-stream` now subscribes de-duplicated single-symbol Klines: each option symbol once and each referenced underlying future once. Batch/metrics IV refresh now fetches option Klines individually, fetches the underlying Kline once per chain batch, then splices cached local option+underlying records into the two-symbol frame required by TQSDK `OPTION_IMPV`. `odm-metrics-worker` groups dirty option tasks by underlying before refreshing so one underlying Kline fetch can serve the chain slice. WSL targeted tests passed: `pytest -s tests/test_quote_streamer.py tests/test_chain_collector.py tests/test_metrics_worker.py -q` (13 passed) and `pytest -s tests/test_api_app.py tests/test_webui_read_model.py -q` (17 passed).
- KLINE-DEDUP performance clarification: The de-dup change improves correctness/resource duplication for local cache and IV refresh, but it does not reduce realtime Kline serial setup call count enough by itself. A local probe found wide multi-symbol Kline serial batches may block on some symbol combinations, so `--kline-batch-size` is available for controlled experiments but defaults to stable single-symbol setup. WSL full suite passed 66 tests after the clarification.
- KLINE-BATCH setting follow-up: Settings/API/WebUI now expose `quote_stream.kline_batch_size` with default `1`; `/api/quote-stream/start` passes it through as `--kline-batch-size`, so operators can test `2` without changing code. This remains an operator-controlled experiment rather than the default because prior probes showed some multi-symbol combinations can stall.
- WEBUI-009 follow-up: realtime health detection now distinguishes subscription setup, closed sessions, opening grace, holiday/no-market-evidence quiet state, stale worker heartbeat, stale TQSDK `wait_update`, and stale Quote writes. The overview shows an `开盘网络` card, and settings status includes the health label. Detection is intentionally conservative: it only raises network warnings during broad commodity trading-session windows after today/current-session futures Quote source time has advanced.
- WEBUI-009 correction: do not use "tomorrow is trading" to gate night sessions because normal Friday night should remain possible before a weekend. The checker now allows Monday-Friday night windows and Saturday early morning continuation, then suppresses only when a statutory holiday is found ahead; normal weekends do not suppress Friday night.
- WEBUI-010 follow-up: realtime workers now attach TQSDK `TqNotify` and record official network notification codes/content into worker progress reports. API/WebUI aggregate `tqsdk_connection_status`, latest notify content, disconnect time, and restore time; the `开盘网络` health result prefers fresh SDK `disconnected`/`reconnecting` notifications during confirmed trading sessions. This is observability only: it relies on TQSDK's built-in reconnect/resend behavior and does not force full resubscription. Verification passed: targeted realtime health/API/quote-stream tests passed 27 tests; WSL full suite passed 75 tests.
- WEBUI-011 correction: realtime subscription startup now owns the empty-database bootstrap case. `/api/quote-stream/start` checks active option count first; if the universe is empty, it creates a temporary TQSDK API, runs one `_discover_and_persist_market` pass in the parent API process, closes that API, and only then starts realtime Quote/Kline workers with `--no-discover`. This keeps worker boundaries clean while preventing new deployments from failing with "no active quote symbols". If discovery fails or returns zero active options, the API returns blocked with a clear message. Verification passed: targeted API/quote-stream/health tests passed 28 tests; WSL full suite passed 76 tests.
- CM-001 correction: dynamic contract management is now an actual realtime behavior, not only a draft spec. Startup discovery is mandatory on every realtime start. During long-lived realtime operation, worker 0 periodically refreshes the TQSDK contract universe into SQLite and all workers periodically reconcile their shard's in-memory Quote/Kline maps against active symbols. Reconciliation keeps unchanged references, removes inactive symbols from local maps, and subscribes only newly active symbols. Tests cover adding a new strike and inactivating an old option without full rebuild. Verification passed: targeted API/quote-stream/health tests passed 29 tests; WSL full suite passed 77 tests.
- Dynamic contract manager clarification: user confirmed that active contract universe changes can include new futures underlyings with option chains and new strike-price contracts on existing chains after underlying price movement. Additions and removals are determined before the next session opens: before night session when available, otherwise before the next morning session, including holiday edge cases. Mid-session contract add/remove is not expected. The v0.1 realtime baseline now implements mandatory startup refresh plus periodic runtime refresh/reconciliation; exact exchange/product-specific pre-session scheduling remains a future precision refinement.

## 2026-05-11

- OPS-001 follow-up: Linux server deployment is now documented in `docs/operations/linux-server-deployment.md`.
- The runbook records the verified release layout (`releases/<short-sha>`, `current`, `shared/`), Python 3.11 `uv` setup, service-readable Python install location, systemd `odm-webui` service, credential owner caveat (`odm` user vs `root` Fernet keys), initial refresh/realtime startup, public test smoke, upgrade, and rollback.
- Project index, release plan, task registry, progress board, and traceability matrix now link or track the deployment documentation.
- WEBUI-012 follow-up: remote public test server hit `/api/quote-stream/stop 500` because stale realtime status cleanup attempted to persist service-state changes while SQLite was locked. Fix makes service-state writes rollback on OperationalError, quote-stream status/stop persistence best-effort when SQLite is busy, and API metrics/log writes best-effort for busy locks. Verification passed locally with `uv run pytest -s -q` at 79 tests, compileall, and agentic-SDLC check; remote hotfix smoke started realtime workers and stopped 1 quote worker plus 1 metrics worker with HTTP 200.
- WEBUI-013 follow-up: remote public test server realtime worker exited at 2026-05-11 20:24 CST because TQSDK `get_quote_list` timed out on a GFEX Quote batch. `quote_streamer` now treats batch Quote subscription failure like Kline batch failure: it falls back to `get_quote` per symbol, counts symbols that still fail, and keeps the long-lived worker running for the rest of the shard. Server config was reduced to 1 worker, Quote shard size 50, Kline batch size 1; hotpatch smoke with `max_symbols=20` reached Quote 20/20 and was stopped cleanly. Verification passed: `uv run pytest -s -q tests/test_quote_streamer.py`, `uv run pytest -s -q tests/test_api_app.py`, compileall, and remote hotpatch smoke.
- WEBUI-014 follow-up: realtime settings now keep near-expiry priority enabled by default and add `quote_stream.contract_months` with `1/2/3/all` choices, default `2`. The WebUI renders it as `1 个 / 2 个 / 3 个 / 全部`; API-started workers pass `--contract-months`; `odm-quote-stream` filters Quote and Kline subscriptions to the selected option expiry-month scope and applies the same scope during dynamic reconciliation. Verification passed in WSL: `uv run pytest -s -q` passed 82 tests; `uv run python -m compileall -q src tests` passed; `bash scripts/agentic-sdlc/check-agentic-sdlc.sh` passed.
- WEBUI-014 UI correction: the overview realtime progress marker and `近N月` summary now render only when `quote_stream.contract_months=all`; when the operator is already scoped to 1/2/3 months, the progress card shows only scoped subscription totals. Worker progress reports now include `contract_months`, and API/WebUI aggregators preserve it. Verification passed: `uv run pytest -s -q tests/test_quote_streamer.py tests/test_api_app.py` passed 26 tests; compileall passed.
- WEBUI-014 overview visibility correction: API startup now persists `quote_stream.contract_months` into service state, and WebUI overview uses worker progress or service state to render `订阅范围：近N个月合约/全部合约`. If the quote-stream message indicates contract discovery is underway, the realtime progress card stage changes to `正在刷新合约列表`. Verification passed: `uv run pytest -s -q tests/test_api_app.py tests/test_quote_streamer.py tests/test_webui_read_model.py` passed 33 tests; compileall passed.
- WSL local startup note: existing `data/option-data-current.sqlite3` can contain a Windows/runtime secret format that the current WSL protector cannot decrypt. API settings and quote-stream start now catch unreadable secret errors and report a blocked credential status instead of raising 500. Local WebUI was started on `http://127.0.0.1:8765`; smoke checks for `/`, `/api/settings`, and `/api/webui/overview` returned 200.
- WEBUI-014 start UX correction: clicking Start no longer blocks the browser until TQSDK contract discovery finishes. `/api/quote-stream/start` records startup state, returns the current status immediately, and launches a daemon thread that refreshes contracts and starts quote/metrics workers. Settings treats messages containing `正在...合约` as a startup state and keeps controls disabled while the background start is active. Verification passed: `uv run pytest -s -q tests/test_api_app.py tests/test_quote_streamer.py tests/test_webui_read_model.py` passed 33 tests; compileall passed.
- WEBUI-014 month-scope restart correction: changing the 1/2/3/all contract-month setting does not require a fresh TQSDK contract discovery when the local active option universe already exists. Startup now reuses existing contracts and lets quote-stream filtering produce the concrete subscription list; TQSDK discovery runs only for empty local universes or explicit forced discovery. This avoids unnecessary server pressure during routine scope changes.
- WEBUI-014 local responsiveness correction: the WSL file-backed database has a large `kline_20d_current` current slice, so WebUI overview reads can be slow on `/mnt/c`. Added current-table read-model indexes and switched WebUI overview/T-quote/runs requests to use request-scoped SQLite read connections for file-backed databases, keeping settings/health/realtime controls from queueing behind a long overview read. Verification passed: targeted API/quote-stream/WebUI/realtime-health tests, compileall, and agentic-SDLC check.
- WEBUI-014 settings-route correction: `/#settings` used to block on the server-rendered root page because the backend eagerly computed a 500-row overview before returning HTML. The root page now ships a lightweight empty initial state; the frontend skips overview loading on settings, lazily loads it when the overview page is active, prevents overlapping overview refreshes, and refreshes overview every 30 seconds instead of every 10 seconds. Verification passed with the same targeted tests, compileall, and agentic-SDLC check.
- WEBUI-014 runtime settings write correction: a settings save during realtime startup hit `/api/settings/api.bind 500` because concurrent WebUI reads and settings commits shared the same SQLite connection. Settings and TQSDK credential writes now use a request-local SQLite connection for file-backed databases plus a process-local write lock, while service-log writes remain best-effort. Verification passed: targeted API/quote-stream/WebUI/realtime-health tests, compileall, and agentic-SDLC check.
- WEBUI-014 contract-month scope correction: user-facing contract-month scope now means each product's nearest underlying contract months, not option exercise months. Quote/Kline subscription selection and near-month counts use the same underlying-contract-month grouping, and the overview table applies the same scoped rows so contracts such as farther CF months no longer appear when the operator selected 2 months. The overview also renders a zero-state collection/subscription skeleton before data arrives, avoiding a blank progress region during startup. Verification passed: `uv run pytest -s -q tests/test_quote_streamer.py tests/test_api_app.py tests/test_webui_read_model.py tests/test_realtime_health.py` passed 42 tests; compileall and agentic-SDLC check passed.
- WEBUI-014 first-run workflow correction: realtime start no longer performs first-run TQSDK contract discovery implicitly. Added `/api/quote-stream/discover-contracts` and a settings button `拉取合约列表`; start now blocks with `未拉取合约列表，无法订阅行情。请先点击“拉取合约列表”。` when the local active option universe is empty. Settings displays active contract count and disables start while contract discovery is running or when no contracts are available. Best-effort service log/state/request metric writes also tolerate retryable SQLite busy/commit anomalies so they do not turn settings or quote-stream controls into 500 responses. Verification passed: targeted API/quote-stream/WebUI/realtime-health tests at 44 tests, compileall, and agentic-SDLC check.
- WEBUI-014 browser review follow-up: settings now lays out `拉取合约列表 / 启动实时订阅 / 停止实时订阅` as one action row. Realtime start requires `quote_stream.contract_list_ready=true`, which is set only after the explicit contract-list pull completes successfully in the current service session; historical active contracts remain visible but do not unlock start by themselves. Verification passed: targeted API/quote-stream/WebUI/realtime-health tests at 45 tests.
- WEBUI-014 contract-manager separation follow-up: the one-shot contract-list pull is now represented as an independent in-process `合约管理器` with `/api/contract-manager` status/start/stop, refresh interval setting, heartbeat, last success/error timestamps, and active option count. Settings renders this as a separate panel between `本地 API` and `实时 Quote/指标 Worker`. Realtime start is blocked unless the contract manager is running and healthy. Verification passed: targeted API/quote-stream/WebUI/realtime-health tests at 45 tests, compileall, and agentic-SDLC check.

## 2026-05-12

- WEBUI-015 pending browser-review follow-up recorded before pausing work. Current observed state on `/#quote`: T型报价 can show cached historical data from 2026-05-09 while the top card says `20日K线 正常`; this is misleading when realtime subscription is not active for the selected chain.
- Required next behavior:
  - 标的汇总表 should be driven by the subscribed/current slice, not by symbols that merely have historical rows in SQLite. Expired or stale historical data has no operator value for the current realtime dashboard.
  - 标的汇总表 should sort by product first, then by delivery/expiry month. Do not sort all products primarily by month.
  - T型报价 should distinguish realtime subscription state from cached historical data. If the selected chain is not subscribed, show `未订阅`; if subscribed, show the latest realtime quote data and timestamp. During market sessions this should update dynamically from the active subscription path.
- Suggested implementation path for the next session: inspect WebUI read-model selectors and overview underlying-summary SQL, add a subscription/current-slice predicate based on quote-stream progress or active subscription reports, adjust ordering to `exchange/product/delivery_month`, then update T型报价 status-card copy and tests so cached `quote_current`/K-line rows cannot produce a false `正常` label when realtime is not running.
- WEBUI-015 completed in WSL2 Ubuntu. `webui/read_model.py` now accepts a realtime quote cutoff and filters overview underlyings to option Quote rows written after the current realtime run started; overview ordering is exchange/product/delivery month rather than latest cached update. `webui/app.py` passes realtime start state into overview/T型报价 and renders T型报价 top status as `未订阅` until the selected chain has fresh Quote rows from this realtime run. Tests added for stale-cache filtering, product/month ordering, and T型报价 freshness status.
- Verification for WEBUI-015: first full-suite run exposed a transient race in an existing contract-discovery test, but immediate isolated rerun passed and the final full WSL run passed: `uv run pytest -s -q` -> 90 passed. Also passed `uv run python -m compileall -q src tests`, `bash scripts/agentic-sdlc/check-agentic-sdlc.sh`, and `uv run python scripts/smoke-local-app.py --database data/tmp-smoke/webui-015-smoke.sqlite3`. Local WebUI is running on `http://127.0.0.1:8765` against `data/option-data-current.sqlite3`; `/`, `/api/webui/overview`, and `/api/webui/tquote` returned 200.
- WEBUI-016 completed: `quote_stream.min_days_to_expiry` is now a runtime setting, CLI flag, API setting, and WebUI field. Realtime Quote/Kline selection filters each product's underlying contract candidates by minimum remaining days before taking `quote_stream.contract_months`, so with default `1`, a 0-day `pt2606` is skipped and a 2-contract scope selects `pt2608` plus `pt2610`.
- Verification for WEBUI-016: `uv run python -m pytest -s -q tests/test_quote_streamer.py tests/test_api_app.py` passed 31 tests; `uv run python -m compileall -q src tests option_data_manager` passed; `uv run python -m pytest -s -q` passed 91 tests; `bash scripts/agentic-sdlc/check-agentic-sdlc.sh` passed; `uv run python scripts/smoke-local-app.py --database data/tmp-smoke/webui-016-smoke.sqlite3` passed.
- OPS-002 completed after `/api/settings` load failure investigation. Added `scripts/odm-server.sh` for local lifecycle management. Use `scripts/odm-server.sh restart` instead of manual `kill`/`uv run odm-webui`; the script asks the API to stop quote-stream and contract-manager workers, writes worker stop-files, terminates stale worker/WebUI processes if needed, starts WebUI detached, and checks `/api/health`.
- WEBUI-017 completed after browser feedback on `/#overview`: the operator expectation is all products, each product's nearest two valid underlying contracts after excluding option-expiry remaining days below 1. `quote_streamer` now excludes contracts using the minimum available option/future remaining-day evidence, so a 0-day option expiry like `FG606` is skipped even if the underlying future delivery is later. `webui/read_model.py` now filters overview underlyings with the same selected subscription scope instead of only rows that have already written current Quote data. Verification passed: targeted quote-stream/WebUI/API suite passed 44 tests; full `uv run pytest -s -q` passed 94 tests; compileall and agentic-SDLC check passed.
- WEBUI-018 completed after browser feedback on unified realtime data outlets. Chosen source of truth is SQLite: the contract manager maintains the local contract universe, realtime worker selection, API/WebUI progress totals, and overview underlyings all derive from the same SQLite-backed subscription scope. The WebUI now shows all intended underlyings immediately after realtime start as `待订阅`; rows become `订阅中` after partial fresh Quote rows and `已订阅` when the selected chain has complete current realtime Quote rows. Verification passed: targeted quote-stream/WebUI/API suite passed 44 tests; full `uv run pytest -s -q` passed 94 tests; compileall and agentic-SDLC check passed.
- WEBUI-020 completed after browser feedback that status-machine decisions must not parse localized message text. `api/app.py` now persists `quote_stream.status` as `blocked`/`starting`/`running`/`stopped`, and `webui/app.py` drives controls/status cards from that enum plus `contract_discovery_running`. Startup no longer performs blocking SQLite pre-calculation in the API process; Quote/Kline totals come from worker reports after launch. Verification passed: targeted quote-stream/WebUI/API suite 45 tests, full suite 95 tests, compileall, agentic-SDLC check, and live local start reached 4 quote worker PIDs with `quote_total=10932` and `kline_total=10932`.
- WEBUI-021 completed after browser feedback that progress could show complete while overview rows still showed un/subscribing. Root cause: progress counted created realtime Quote/Kline references, but overview row status counted fresh Quote writes after startup. The fix makes overview row status follow enum-backed subscription lifecycle, aligns scoped summary counts to worker progress, enables runtime SQLite WAL configuration for concurrent read/write, and keeps detailed Quote/Kline coverage metrics separate. Verification passed with targeted quote-stream/WebUI/API/WebUI-app tests at 47 tests, compileall, and live local evidence: 128 overview rows all `已订阅`, including `CZCE.FG607`/`CZCE.FG608` and `CZCE.MA607`/`CZCE.MA608`, after progress reached 11110/11110 Quote and 11110/11110 Kline.

## 2026-05-13

- WEBUI-022 completed after live browser feedback that T型报价 could appear static even though subscriptions were marked complete.
- Root cause: Quote refs were established, but the worker did not keep writing visible Quote rows while it was still creating thousands of Kline objects; the first startup snapshot also used per-symbol SQLite commits, which made startup writes expensive.
- Fix: `quote_streamer` bulk-writes Quote snapshots, refreshes Quote snapshots every 5 seconds during Kline setup, and keeps startup snapshot writes out of the metrics dirty queue. `QuoteRepository` and `InstrumentRepository` now have bulk update paths. `realtime_health` now uses the freshest actual DB quote write when worker `wait_update` stats are stale. WebUI/T型报价 requests remain read-only from the realtime data-source perspective; they only choose what chain to display.
- Verification: targeted realtime tests passed 29 tests; full `uv run pytest -s -q` passed 99 tests; compileall and agentic-SDLC check passed; live local WebUI remained on `http://127.0.0.1:8765`; active night-session `SHFE.al2606` source time advanced from `2026-05-13 23:24:21` to `23:25:38`, while closed-session contracts such as `CZCE.CF607` correctly kept source time near `22:59:59`.

## 2026-05-14

- New active work: CR-2026-05-14-MONEYNESS-KLINE-FILTER / SPEC-002.
- User-confirmed design: contract manager remains low-frequency for candidate contract universe; intraday moneyness filtering is separate and recalculates during market movement.
- Quote subscriptions should remain broad within existing contract-month scope. Kline subscriptions are filtered by selected moneyness ranges.
- Runtime Kline behavior is sticky during the trading day: add newly matching Kline symbols but do not release symbols that move out of range until realtime restart/new-session/full-scope rebuild.
- Trading-session logic must be contract/product granular, not exchange granular, because products on the same exchange can have different night-session rules.
- WebUI should display trading-time state in the underlying/contract list and T型报价.
- User emphasized architecture quality: keep the feature modular, protect system performance from cumulative feature complexity, add strong aggregate logs/counters for operations, and require a trial-run inspection to ensure existing Quote freshness, metrics, contract manager, SQLite, and WebUI behavior are not harmed.
- SPEC-002 implementation completed in this session. Added `moneyness.py` and `trading_sessions.py`, wired Quote/Kline selection so Quote remains broad and Kline follows selected `otm/atm/itm`, corrected reconciliation so moneyness no longer releases active sticky Klines intraday, and added running sticky expansion skip counters for out-of-session and missing underlying price cases.
- API/CLI/WebUI settings now expose moneyness filter and recalculation seconds. Worker progress/API/WebUI aggregate current match count, sticky Kline count, added Klines, last recalculation, and skip reasons. Overview underlyings and T型报价 display trading-session state from each underlying Quote `trading_time`.
- Verification passed: targeted SPEC-002 suite `uv run pytest -s -q tests/test_moneyness.py tests/test_trading_sessions.py tests/test_quote_streamer.py tests/test_api_app.py tests/test_webui_read_model.py` passed 60 tests; `uv run python -m compileall -q src tests option_data_manager` passed; full `uv run pytest -s -q` passed 112 tests; `bash scripts/agentic-sdlc/check-agentic-sdlc.sh` passed.
- Remaining acceptance: run a representative live market-session trial and inspect realtime Quote freshness, sticky Kline growth, metrics worker behavior, service logs, WebUI responsiveness, and SQLite lock symptoms before final operational signoff.
- Browser-review correction: live startup showed Kline progress `0/0` because initial filtered Kline selection could run before underlying Quote prices existed, then heartbeat recalculation counted all OTM candidates as out-of-session and kept the target at zero. Fixed by recomputing initial Kline targets after the first Quote snapshot and by separating heartbeat target counts from trading-session-gated eligible additions. Also locked all realtime Worker settings while running. Verification: `uv run pytest -s -q` passed 113 tests; compileall passed; local live restart on `http://127.0.0.1:8765` reached Quote `11126/11126`, Kline target `5009`, moneyness `otm`, and 4 worker reports.
- Timeout watchdog follow-up: a fresh local live session on the new code reached `quote_subscribed=11126/11126`, `kline_subscribed=320/5008`, and 4 running worker PIDs `50747-50750`. Each worker continued advancing through `kline_subscribing` with a changing `current_kline_symbol`, no Kline timeout hits, and no contract-manager involvement in the stall pattern. This confirms the earlier freeze was caused by a blocking synchronous Kline call path, not by worker restarts or contract-manager failure.
- Trading-session display follow-up: browser review found overview rows such as `SHFE.ad2606` showing `休市` while Quote `source_datetime` was actively advancing at night. Root cause was relying on theoretical TQSDK `trading_time.night`, which can be empty even during a live night session. `trading_sessions.py`, WebUI read-model rows, and running moneyness session gating now treat fresh underlying Quote source time as authoritative live evidence before falling back to `trading_time`. Verification passed with targeted trading-session/WebUI/quote-stream tests at 45 tests plus compileall; live API showed SHFE ad/ag/al as `交易中` and stale/non-night CZCE AP as `休市`.
- No-Quote-time state follow-up: user clarified the distinction between startup uncertainty and subscribed closed-session evidence. Overview rows now show `待行情` only before the row is fully subscribed; if the row is `已订阅` and still has no underlying Quote time, it shows `休市`. Verification passed with targeted WebUI read-model/trading-session/quote-stream tests at 48 tests.
- MM-T018 reference-price fallback follow-up: user asked whether TQSDK has a per-option moneyness field. Local TQSDK 3.9.5 inspection confirmed no ready-made moneyness field, but Quote exposes `settlement`, `pre_settlement`, and `pre_close`. Implemented shared `underlying_reference_price` for realtime Kline selection and T型报价 masking: latest price, bid/ask midpoint, close/settlement, then previous settlement/close. Added tests for no-night/closed-session `last_price=None` with `pre_settlement` still producing OTM Kline targets instead of `0/0`. Browser restart initially failed because worker SQLite rows were plain tuples and `dict(row)` crashed before Quote subscription; fixed by routing cursor rows through `_dict_rows()`, added tuple-row test coverage, and verified live local realtime reached Quote `11126/11126`, Kline `5496/5496`, with 4 workers and 0 Kline errors. A second live observation caught Saturday-night theoretical-session leakage; trading-session gating now excludes Saturday night while preserving Friday-night/Saturday-early continuation. Also made `/api/quote-stream/start` respect an already configured `quote_stream.report_dir` to keep tests and local runtime reports isolated.
- MM-T018 progress口径 follow-up: browser review found the overview could show impossible realtime progress such as Kline `6225/2160` and AP610 `14/14` after running-phase reconciliation. Root cause was reporting held sticky Kline refs as subscribed while replacing the target list with a smaller reconciliation candidate list. `quote_streamer` now returns the moneyness-selected target list from reconciliation and counts subscribed Klines only when the ref is still in that target scope. Added regression coverage and verified local restart: Quote `11126/11126`, Kline `5496/5496`, AP610 `36/36`, 0 Kline errors; full suite passed 132 tests.
